# Project Dev Plan

## 2026-03-10

- Added `scripts/setup-vscode.ps1` to install the recommended VS Code extensions and merge workspace settings.
- Added `.vscode/extensions.json` so the repository recommends the UI, formatting and Playwright tooling needed for this project.
- Expanded `.vscode/settings.json` with shared editor defaults while preserving the existing repository-specific settings.
- Added `Project_todos.md` as a checklist for follow-up actions related to the VS Code bootstrap.
- Saved the new premium marble theme plan in `Plans/Premium Marble - Dark Gold Theme Replan.md` and registered it in `Plans/Plan History.md`.
- Reworked the shared design tokens in `styles/tokens.css` around a darker black-led palette, marble surfaces and muted dark-gold accents.
- Applied the premium marble / dark-gold shell consistently in `styles/public.css` for the public header, homepage panels, quote/contact surfaces and footer.
- Applied the same premium system in `styles/workspace.css` so auth and dashboard shells inherit the same brand language with calmer operational cards.
- Updated `tests/playwright/public-redesign.spec.js` to validate the evolved homepage hero language and keep regression coverage aligned with the new visual direction.
- Re-ran generated page verification, API tests and mobile Playwright coverage to confirm the theme pass did not break public or workspace behaviour.
- Updated `AGENTS.md` with a permanent platform-direction rule: build current web features so they stay reusable for the planned future Android/iOS app.
- Added the matching checklist item in `Project_todos.md` so app-readiness is reviewed continuously, not only at planning time.
- Reworked the homepage brand hierarchy in `index.html` so the header is cleaner, `title.png` sits in the hero and the `Account` card now owns login plus the public route links.
- Updated `styles/public.css` to support the new homepage-only shell: hidden desktop title in the header, new hero lockup treatment, and the moved account navigation block.
- Updated `tests/playwright/mobile-smoke.spec.js` and `tests/playwright/public-redesign.spec.js` so regression coverage follows the new homepage structure instead of the old header utility layout.
- Re-ran the Playwright mobile/desktop suite after the homepage restructure and restored tracked test artifacts back to the repository state.
- Saved `Plans/Professional Client Proposal Quote Form Plan.md` to define the professional quote intake model, phased form UX and portable `client_proposal_quote` data contract.
- Registered the quote form plan in `Plans/Plan History.md` and linked the next implementation tasks into `Project_todos.md`.
- Added explicit `HOST` handling in `server.js` so deploys no longer rely on implicit bind behaviour for `app.listen`.
- Set `HOST=127.0.0.1` in `ecosystem.config.js` for PM2 and `HOST=0.0.0.0` in Docker compose files so both droplet and container flows stay predictable.
- Updated `README.md` and the follow-up checklist with the new host-binding rule and the need for `pm2 restart --update-env` after deployment.
- Reworked the shared site shell so public pages, legal pages and workspace pages all use the same `logo | title | account/nav` header structure instead of the earlier homepage-specific exception.
- Updated `brand.js`, `scripts/publicPages.shared.js`, `scripts/publicPageRenderer.js` and the manual HTML files so the guest-facing auth label is consistently `Account`.
- Regenerated the public service/location pages and re-ran the API + Playwright suites after the shell change to verify desktop and mobile behaviour.
- Added `/healthz` in `app.js` as a simple process-level health endpoint with `no-store` caching, alongside the existing `/api/v2/health`.
- Extended the legacy app tests and deploy documentation so droplet checks can use `/healthz` directly, while leaving the Nginx upstream on `127.0.0.1:3000` because that is the listener confirmed by `ss`.

## 2026-03-11

- Replaced the old manager workspace hero/session block with a new operational overview board in `manager-dashboard.html` made of `Company Events`, `Mail Box` and `Available Options`.
- Added anchor targets for the main manager sections so the new overview cards can jump directly into projects, quotes, services, materials, clients, staff, estimates and both chat modes.
- Extended `manager-dashboard.js` with overview rendering helpers for company activity, mailbox previews and role-aware available options, using real loaded data rather than static placeholder text.
- Kept the new mailbox top card light on bootstrap by loading only thread summaries first, then leaving full message history to the existing lower lazy-loaded inbox/chat sections.
- Added manager-specific workspace styling in `styles/workspace.css` so the new overview board uses the same dark-gold operational shell and responsive stacking on desktop and mobile.
- Updated `tests/playwright/mobile-smoke.spec.js` so the manager dashboard regression suite checks the new overview structure and summary counters.
- Re-ran `cmd /c npm run test:ci` and `cmd /c npm run test:e2e:mobile` after the manager dashboard overview refactor and kept both gates green.
- Diagnosed the droplet deploy logs and confirmed the process now binds to `127.0.0.1:3000`, while the remaining failed `curl` is consistent with an immediate post-restart timing race rather than a broken listener.
- Recorded that local live QA through MCP Playwright is currently blocked on the workstation because the expected Chrome executable is missing, so browser-based live inspection needs a local Playwright browser install first.
- Saved `Plans/Full Stabilization, Mobile Sizing, Performance And Growth Plan For The Site.md` and registered the plan in `Plans/Plan History.md` so the next stabilization/performance work has a single tracked reference.
- Added semantic contrast tokens in `styles/tokens.css` (`text-on-light`, `text-on-dark` and muted variants) to enforce the rule that light surfaces keep dark text and dark surfaces keep gold text.
- Tightened public mobile sizing in `styles/public.css`: smaller header logo/title proportions, denser `390px`/`640px` card spacing and less dominant first-fold hero content on phones.
- Applied the same contrast and mobile-sizing cleanup in `styles/workspace.css`, including smaller client/manager board spacing and more controlled heading sizes on narrow screens.
- Rebuilt `client-dashboard.html` around the shared workspace operations board, replacing the older hero/session top with `Project Status`, `Mail Box` and `Available Options`.
- Extended `client-dashboard.js` with lightweight project/mailbox overview rendering and thread-summary loaders so the client top board stays informative without forcing full message-history fetches at bootstrap.
- Updated `tests/playwright/mobile-smoke.spec.js` so mobile regression coverage checks the new client workspace top board and mailbox counters.
- Switched `tests/playwright/playwright.config.js` to Playwright-managed desktop Chromium instead of relying on a local Chrome channel, which is the better technology choice for reliable repeatable UI checks in this repo.
- Updated README and the cutover checklist so droplet deploy instructions restart through `ecosystem.config.js --update-env`, wait briefly after PM2 restart and validate via `/healthz`.
- Re-ran the available local validation entry points and confirmed the current blocker is environmental: both `npm run test:ci` and `npm run test:e2e:mobile` still fail locally with `spawn EPERM`, so the repo config is improved but the workstation runner still needs fixing.
- Adjusted the shared dark-surface text tokens in `styles/tokens.css` from pale gold toward a deeper dark-gold range so headings, nav and workspace text on black surfaces now match the premium direction more closely.
- Saved `Plans/Live QA, Asset Optimization And Richer Gold Tuning.md` and registered it in `Plans/Plan History.md` to track the runtime asset and live-QA pass explicitly.
- Added `scripts/asset-optimization.config.js` and `scripts/optimize-assets.js`, using `sharp` to generate repeatable AVIF/WebP/fallback variants for brand assets and the runtime `Gallery/premium` set.
- Updated `package.json` so `npm run optimize:assets` is a first-class task and `generate:public-pages` now rebuilds optimized runtime media before regenerating public HTML.
- Generated a tracked `asset-manifest.js` plus optimized brand assets under `assets/optimized/brand/` and runtime gallery assets under `Gallery/premium/` to replace missing/heavy runtime sources.
- Extended `runtime.js`, `brand.js`, `scripts/publicPages.shared.js`, `scripts/publicPageRenderer.js` and `gallery.js` so public/runtime media can resolve optimized variants and render through `<picture>` instead of a single heavy source.
- Updated the manual shell pages and generated-page renderer to load `asset-manifest.js`, use optimized brand images with intrinsic dimensions and point JSON-LD brand images at the optimized title asset.
- Updated `styles/base.css` and `styles/tokens.css` so picture-based media keeps existing layout geometry, dark surfaces use a richer premium gold, and the new asset sizes reduce first-fold pressure on mobile.
- Added AVIF to the tracked Nginx static asset regex so the deploy config can cache the new optimized image format directly when the site config is refreshed.
- Extended Playwright smoke/regression coverage to assert optimized brand pictures are present and that homepage/auth/dashboard shells keep zero horizontal scroll on mobile.
- Added `deploy/LIVE_QA_CHECKLIST_PC_MOBILE.md` as the repeatable desktop/phone verification checklist for post-deploy evidence capture.
- Removed the obsolete `RELEASE_NOTES_design-lock-v1.md` document and replaced it with `Project_Web_Design_Plan.md` so the repo now has a stable three-file loop: target design state, open issues and completed work.

## 2026-03-12

- Saved `Plans/Pelna Analiza Projektu I Plan Naprawy - Usprawnien.md` and registered it in `Plans/Plan History.md` so the stabilization/modularization wave has a tracked project plan.
- Added `test-results/` to `.gitignore` and stopped carrying `.last-run.json` as a tracked repo file, because it is a volatile Playwright artifact that kept dirtying the worktree after validation.
- Replaced the fragile `node --test` CLI usage in `package.json` with `scripts/run-api-v2-tests.js`, using `node:test` programmatic execution one file at a time to avoid the workstation `spawn EPERM` blocker without breaking per-file test isolation.
- Updated `scripts/run-playwright.js` to keep the external static server path but call the Playwright CLI programmatically without the broken `from: 'user'` argument handling.
- Verified that the remaining local Playwright blocker is environmental: Node cannot spawn worker processes or the Chromium executable on this workstation, so the failure is outside app logic.
- Moved duplicated overview/mailbox helpers (`titleCase`, `formatDateTime`, `createOverviewEntry`, `renderMailboxPreviewList`) into `runtime.js` and rewired both `client-dashboard.js` and `manager-dashboard.js` to consume the shared runtime helpers.
- Reduced `apps/mobile-v1/App.js` by extracting reusable mobile API/session loading helpers into `apps/mobile-v1/src/api.js` and `apps/mobile-v1/src/useApiList.js`, which improves parity with the web-side shared-client direction.
- Saved `Plans/Plan Naprawy Bledow Znalezionych Przez SonarQube.md` and linked it with `Project_todos.md` so SonarQube cleanup now has a tracked triage and execution path.
- Added `scripts/sonar-export.sh` as the repo-native Linux export path for SonarQube issues, quality gate and measures so the next cleanup pass can work from real issue data instead of inferred hotspots.
- Continued the Sonar/stability cleanup by splitting `routes/manager.js` so `staff/search/seed`, `services/materials` and `estimates` now register through dedicated subrouters under `routes/manager/` while quote/project/media endpoints stay in the main router.
- Reduced `apps/mobile-v1/App.js` further by extracting React Native screen components into `apps/mobile-v1/src/screens.js` and moving the shared mobile style sheet into `apps/mobile-v1/src/styles.js`, leaving the app shell focused on session and tab orchestration.
- Re-ran `npm run verify:generated` and `npm run test:ci` after the manager-route split and mobile-v1 screen/style extraction; both now pass with the refactor in place.
- Re-checked `npm run test:e2e:mobile` and confirmed the only remaining failure is still the workstation-level Playwright `spawn EPERM` blocker, not an application regression from this refactor.
- Rebuilt the homepage shell in `index.html` so it now starts with a three-part `header.png` board: left account panel, central artwork panel and right menu panel.
- Split the homepage flow into alternating light/dark bands in `styles/public.css`, matching the requested black/white background rhythm while keeping existing `projects`, `gallery`, `services`, `contact` and `quote` anchors intact.
- Updated homepage Playwright regression coverage in `tests/playwright/public-redesign.spec.js` and `tests/playwright/mobile-smoke.spec.js` so the tests validate the new board layout instead of the older `brand-title-link` header.
- Tuned the live proportions of the new homepage board in `styles/public.css`: smaller `header.png` height, a full dark top band and stronger balance between account, art and menu panels after the first production screenshot review.
- Saved `Plans/Redesign Publicznego Shellu Pod title.png.md` and registered it in `Plans/Plan History.md` before rolling out the new shared public shell.
- Replaced the temporary homepage-only `header.png` composition with a shared public shell built around a centered `title.png` board and a lower inline utility strip.
- Updated `scripts/publicPages.shared.js` so all public navigation now resolves in the fixed order `About Us | Gallery | Quote | Contact | Account`.
- Reworked `scripts/publicPageRenderer.js` so generated service, location and legal pages render the same `title.png` shell and no longer diverge from the manual pages.
- Replaced the manual public-page headers in `index.html`, `about.html`, `gallery.html`, `contact.html`, `quote.html`, `privacy.html`, `cookie-policy.html` and `terms.html` with the same shared shell markup.
- Extended `site.js` with a real inline public login/session strip that posts to the existing auth flow, stores session state and swaps to an account/session view when the user is already logged in.
- Added the shared shell styles to `styles/public.css`, including responsive handling for the centered `title.png` board, the inline login form and the right-aligned public nav.
- Regenerated the public service/location pages, then re-ran `npm run verify:generated` and `npm run test:ci`; both passed after the new shell rollout.
- Re-ran `npm run test:e2e:mobile` and confirmed the remaining failure is still the known workstation-level `spawn EPERM` issue rather than a regression from the new public shell.
- Tightened the shared public shell proportions in `styles/public.css` so `title.png` now sits in a slimmer top bar and the inline login/menu strip reads as a lower quick-access band instead of two oversized panels.
- Regenerated the optimized `title` brand asset and manifest from the new thin `title.png` source, updating the runtime dimensions from `1800x412` to `1536x232` so the public shell now renders the intended narrow brand strip instead of the older taller board.
- Updated the static public pages plus regenerated service/location pages to use the new `1536x232` `title.png` intrinsic dimensions, keeping the quick-access login/menu strip visually aligned with the slimmer shell.
- Tightened the public shell again in `styles/public.css` so the upper `title` bar and the lower `login + menu` strip behave like a true two-tier quick-access header across homepage and all public pages.
- Updated `deploy/LIVE_QA_CHECKLIST_PC_MOBILE.md` so the post-deploy checks now validate the new thin `title` strip and lower quick-access login/menu band instead of the older `logo | title | account/nav` shell wording.
- Added `sonar-project.properties` with targeted `sonar.cpd.exclusions` for static/generated public HTML outputs, so SonarQube duplication metrics stop flagging intentional shared public shell markup while still checking scripts and styles normally.

## 2026-03-15

- Saved `Plans/Redesign Od Zera - Cala Strona, Design, IA I SEO.md` and registered it in `Plans/Plan History.md` as the tracked full redesign/IA/SEO plan for the public website.
- Reworked the shared public shell inputs and nav source so the sticky header now follows the final brochure IA: `Home`, `About`, `Services`, `Gallery`, `Quote`, `Contact`, `Account`, with compact mobile auth toggle behaviour handled in `site.js`.
- Rebuilt the manual public brochure pages (`index.html`, `about.html`, `services.html`, `gallery.html`, `contact.html`, `quote.html`) around one quote-first structure: clear intro, fewer duplicated top-level elements, stronger CTA hierarchy and a consistent dark-shell / light-card visual rhythm.
- Added `services.html` as a first-class brochure hub page so service discovery no longer relies only on footer links or dispersed homepage sections.
- Rebuilt the legal pages (`privacy.html`, `cookie-policy.html`, `terms.html`) into the same shared shell and footer system so they no longer drift away from the main site design or navigation.
- Updated `sitemap.xml` to include `services.html` and refreshed the manual page metadata so the new brochure IA has matching canonical, title, description and breadcrumb structure.
- Updated `Project_Web_Design_Plan.md` so the design source of truth now reflects the sticky `title.png` shell, the quote-first brochure IA and the compact mobile auth/menu behaviour.
- Updated Playwright public/mobile expectations so the regression suite now checks the new menu set, the `services.html` route and the compact mobile auth flow instead of the previous shell assumptions.
- Regenerated the service/location pages after the shared shell/nav changes so the generated SEO pages now inherit `Home / About / Services / Gallery / Quote / Contact / Account` and breadcrumb links back through `services.html`.
- Aligned `auth.html`, `client-dashboard.html` and `manager-dashboard.html` with the new `Home / About / Services / Gallery / Quote / Contact` menu language and the slimmer `title.png` intrinsic dimensions so the shell no longer mixes old and new nav labels.
- Updated `deploy/LIVE_QA_CHECKLIST_PC_MOBILE.md` so post-deploy checks now include `services.html`, a legal page and the new sticky/compact shell behaviour instead of the older quick-access wording.
- Re-ran `node --check` for the touched shell/generator/runtime files, `npm run generate:public-pages`, `npm run verify:generated` and `npm run test:ci`; the generator, verification and API suite all passed after the redesign rollout.
- Performed a repo-wide performance review focused on the slowest manager/client dashboard paths, manager search/list routes, gallery runtime updates, public session validation and the mobile-v1 polling loop.
- Saved `Plans/Checklist Usprawnien Wolnego Lub Nieefektywnego Kodu.md` and registered it in `Plans/Plan History.md` as the execution checklist for slow or inefficient code findings.
- Added the concrete performance remediation items to `Project_todos.md`, covering manager list payload weight, expensive search filters, eager thread preloads, heavy dashboard rerenders, gallery rebuilds, mobile polling cadence and public session validation overhead.
- Changed `routes/manager.js` so `/api/manager/projects` now defaults to lightweight list payloads without full media arrays, while preserving explicit `includeMedia=true` support.
- Removed the eager thread-summary preload from `client-dashboard.js` bootstrap so direct/group mailbox summaries stay truly lazy and align with the existing section-visibility flow.
- Extended `tests/api-v2/manager-projects-include-media.test.js` so the new default lean manager-project list behaviour is covered by API regression tests.
- Narrowed the manager project free-text search in `routes/manager.js` to project-owned fields only (`title`, `location`, `description`) so the common list path no longer pushes broad `LOWER(... LIKE %...%)` filters across joined relations.
- Added a shared keyed-list sync helper in `runtime.js` and rewired the `projects`, `threads` and `messages` sections in both `client-dashboard.js` and `manager-dashboard.js` to patch existing DOM nodes instead of fully clearing and rebuilding those containers on every update.
- Extended the manager project API regression suite with a search-shape assertion so the narrowed `q` path is protected in tests.
- Reworked the manager seed action in `manager-dashboard.js` so it now refreshes only the domains the seed actually changed, instead of always reloading projects, services and materials together.
- Replaced the gallery's full `stage` and `projectStrip` rebuilds with keyed DOM sync in `gallery.js`, so project changes now update only the cards and chips that actually changed while preserving active-strip state and roller transforms.
- Re-ran `node --check manager-dashboard.js`, `node --check gallery.js` and `cmd /c npm run test:ci`; all passed after the seed/gallery performance changes.
- Replaced the 1-second global polling heartbeat in `apps/mobile-v1/App.js` with a nearest-due timeout scheduler that re-arms only when a poller is due or the app returns to the foreground.
- Added a short session-scoped TTL cache around `/api/auth/me` in `site.js`, so the shared public shell can reuse a fresh authenticated user snapshot instead of revalidating on every public page load.
- Re-ran `node --check apps/mobile-v1/App.js`, `node --check site.js` and `cmd /c npm run test:ci`; all passed after the mobile scheduler and public-session cache changes.
- Updated the public sticky shell so `data-nav-menu` starts hidden in HTML, `site.js` manages `hidden`/`aria-hidden` alongside the existing toggle state, and the menu no longer renders as a permanently visible bar in the `title.png` header.
- Changed the public menu trigger styling in `styles/public.css` so the control reads as a clean three-line hamburger button while keeping the text label only for accessibility.
- Regenerated the manual and generated public pages, then re-ran `npm run generate:public-pages`, `npm run verify:generated`, `node --check` for the touched shell/runtime/tests and `npm run test:ci`; all passed after the header menu toggle fix.
- Added a public-site typography override in `styles/public.css` so light cards, legal/content shells, form text, footer links and CTA labels now render in the same gold family as the dark surfaces instead of falling back to dark text on the pale cards.
- Re-ran `npm run test:ci` after the public gold-typography pass; the generated-page verification and full API suite both still passed.
- Saved `Plans/Pelna Analiza Projektu - Audit 360 + Roadmapa Rozwoju.md` as the current full-project audit covering architecture, product operations, public UX/SEO, performance hotspots and the decision-ready roadmap.
- Re-grounded the audit in the actual repository state by reviewing the current Express app composition, domain models/associations, route surface, mobile-v1 structure, tracker documents and the biggest code hotspots by line count.
- Re-ran `npm run verify:generated` and `npm run test:ci` as the baseline technical validation for the audit; both passed, while the already known Playwright workstation blocker remains outside this repo-level gate.
- Added the missing audit-derived backlog items into `Project_todos.md`, including CRM lifecycle, project stages/tasks, durable activity feed, explicit estimate approval flow, shared web/mobile contracts and unifying the public content model.
- Kept the current narrower `title.png` variant in place and tuned `styles/public.css` instead of reverting the asset, reducing the rendered brand width and adding breathing room so the sticky shell stays calmer even with the tighter crop.
- Re-ran `npm run test:ci` after the non-destructive `title.png` shell-tuning pass; generated-page verification and the API suite both still passed.
- Extracted the remaining manager `quotes` and `projects/media` endpoints into `routes/manager/quote-routes.js` and `routes/manager/project-routes.js`, reducing `routes/manager.js` to a thin composition layer around shared helpers and subrouters.
- Continued slimming `apps/mobile-v1/App.js` by moving the poller scheduler into `apps/mobile-v1/src/usePollerScheduler.js` and the login/tab shell UI into `apps/mobile-v1/src/session-shell.js`.
- Re-ran `npm run test:ci` after the route/mobile refactor and kept the suite green.
- Replaced the old workspace/auth header markup in `auth.html`, `client-dashboard.html` and `manager-dashboard.html` with the same sticky `title.png` public shell used on brochure pages, keeping session-aware account and hamburger menu behaviour consistent after login.
- Scoped the legacy workspace-header overrides in `styles/workspace.css` to non-public-shell headers so the new shared shell is not visually distorted on logged-in pages.
- Re-ran `npm run test:ci` after the header unification change and kept the suite green.

## 2026-03-16

- Saved `Plans/Spojnosc Logiki Sesji, Headera I Nawigacji Na Calej Stronie.md` and registered it in `Plans/Plan History.md` so the session/header consistency pass has its own tracked implementation plan.
- Centralised session-state rendering in `site.js`, adding shared guest-only, user-only and account-settings hooks plus one visibility model for public pages, `auth.html` and both workspace dashboards.
- Removed the inline `Open Account` action from the shared header shell and changed the authenticated public header state to one route label (`Account`) plus `Log out`, while hiding header auth controls on `auth.html` and workspace pages once a user is signed in.
- Added protected-route handling in `site.js` so guest access to workspace pages redirects to `/auth.html?next=...&reason=session` and wrong-role visits redirect to the correct dashboard.
- Simplified `auth.html` and `auth.js` so the signed-in state now behaves as account settings plus logout only, without extra dashboard launcher buttons.
- Replaced the old `Auth Page` body action in `client-dashboard.html` and `manager-dashboard.html` with `Account Settings`, keeping only authenticated actions in workspace body controls.
- Updated the shared public renderer and regenerated the service/location pages so the new session hooks and session-state shell rules stay aligned with manual pages.
- Extended Playwright regression coverage to assert the new session-state behaviour: no `Open Account`, no header account duplicate on signed-in account/workspace pages, and redirect to auth for guest access to protected client workspace routes.
- Re-ran `node --check` on the touched runtime/test files, then `npm run verify:generated` and `npm run test:ci`; all passed after the session/navigation consistency pass.
- Consolidated all historical documents from `Plans/` plus `Plans/Plan History.md` into one root-level `todosv2.md`, preserving the merged roadmap and former plan chronology in a single place.
- Removed the old standalone plan markdown files from `Plans/` so planning now lives in `todosv2.md`, while `Project_todos.md` remains the active checklist and `Project_Dev_plan.md` remains the execution log.
- Removed `ms-vscode.live-server` from `.vscode/extensions.json` and `scripts/setup-vscode.ps1` after confirming the repo no longer uses it; the supported local flow remains the app server plus Playwright.
- Audited the remaining `multer@1.x` usage, confirmed upload handling is centralized in `utils/upload.js` plus four route surfaces, and documented a dedicated `multer 1.x -> 2.x` migration plan directly inside `todosv2.md`.
- Kept the updated design-system rule in `Project_Web_Design_Plan.md` that light backgrounds currently use gold text, with the explicit follow-up that live QA must continue checking readability on light cards and form surfaces.
- Reviewed the current CSS split and kept `styles/public.css` / `styles/workspace.css` intact for now; the measured hotspot is still `styles/base.css`, so the next worthwhile cleanup remains moving more layout ownership out of base rather than creating extra section files prematurely.
- Removed the obsolete `brand-mark-link` / `brand-title-link` header-lockup block from `styles/base.css` after confirming those selectors are no longer used anywhere in the runtime, then re-ran `npm run test:ci` to keep the cleanup verified.
- Updated `scripts/setup-vscode.ps1` to work on the current local machine by preferring `code.cmd` over `Code.exe` and replacing the PowerShell-7-only `ConvertFrom-Json -AsHashtable` usage with a PowerShell-5.1-safe recursive conversion helper, then re-ran the bootstrap successfully.
- Reworked the public gallery source model so homepage and `gallery.html` can switch to `data-gallery-source="services"` and load grouped image sets from service subfolders instead of the older mixed inline project payload.
- Added `fetchServiceGalleryFoldersCached()` in `utils/publicGallery.js`, exposed new `GET /api/gallery/services` and `GET /api/v2/gallery/services` routes, and wired `app.locals.galleryPath` so both legacy and v2 contracts can read the new folder-based gallery structure.
- Created real service gallery folders under `Gallery/` (`bathrooms`, `kitchens`, `interiors`, `exteriors`) and populated them with representative completed-work images copied from the existing premium gallery set.
- Extended `gallery.js` with a service-gallery mode and a reusable fullscreen lightbox overlay, so clicking any gallery image now opens a closable fullscreen view while generated service/location pages remain compatible with their inline case-study payloads.
- Updated `index.html`, `gallery.html`, `styles/public.css`, `api/v2/README.md`, and the legacy/v2 gallery tests to reflect the new service-led gallery wording, overlay styling and API surface.
- Re-ran `node --check` on the touched runtime/API/test files and `cmd /c npm run test:ci`; both the generated-page verification and API suite passed after the folder-based gallery/fullscreen rollout.
- Removed `Project_Web_Design_Plan.md` to clear the previous visual-direction brief before planning the next site redesign from a clean baseline, and updated `Project_todos.md` to track the replacement source-of-truth document instead.
- Rebuilt `index.html` from a stacked multi-section homepage into one menu-driven card stage: fixed service rail on the left, one central active card selected from the menu, fixed location rail on the right, and a lower studio contact strip inspired by the new wireframe direction.
- Extended `site.js` so homepage menu items and in-card buttons can switch the active homepage card in place through `data-home-card-target` / `data-home-card-trigger`, while keeping the real page routes as fallback links for non-home navigation.
- Added new homepage-only styling in `styles/public.css` so the desktop header now reads like a large brand board plus stacked login/menu controls, while the new single-card stage stays responsive and collapses cleanly on tablets and phones.
- Updated `tests/playwright/public-redesign.spec.js` to reflect the new homepage card-board behaviour instead of the older long-scroll section assumptions.
- Tightened the single-card homepage after live/local review by widening the desktop centre card, narrowing both side rails, reducing the visible service/location item counts, and moving the active centre card ahead of the side rails on mobile so the first screen stays useful on phones.
- Reworked the mobile homepage header so the `title.png` board takes a full row above the auth/menu controls, improving proportions and making the top of the page read more like the wireframe instead of a tiny compressed shell.
- Removed the entire active style layer on request by resetting `styles/tokens.css`, `styles/base.css`, `styles/public.css`, `styles/workspace.css` and root `styles.css` to a neutral no-theme baseline.
- Removed legacy design/planning documents tied to the previous visual direction (`DESIGNER_BRIEF_LEVELLINES.md`, `todosv2.md`, `dev_plan.md`, `todos.md`) so the next site look can start from a clean planning baseline.
- Implemented the vNext one-release rebuild after the style wipe by recreating the full design system in `styles/tokens.css`, `styles/base.css`, `styles/public.css` and `styles/workspace.css` (dark shell, light cards, gold typography, responsive spacing and sticky header shell).
- Reworked `index.html` into a true multi-page homepage flow with one dominant main card plus supporting sections and quote CTA, and removed homepage-only hash/card switching from `site.js`.
- Kept the sticky `title.png` shell across public/auth/workspace pages with always-visible inline login fields for guest state on mobile and desktop, plus hamburger-driven vertical menu routing to dedicated pages.
- Updated generated SEO pages by changing `scripts/generate-service-pages.js` and `scripts/generate-location-pages.js` to stop rendering the old `studio-board` variant and use the shared brochure renderer path (`hero + sections + contact + consultation`).
- Regenerated public outputs via `npm run generate:public-pages` and verified generated-file sync with `npm run verify:generated`.
- Updated Playwright expectations in `tests/playwright/public-redesign.spec.js` and `tests/playwright/mobile-smoke.spec.js` for the new homepage/shell/session behavior and reran `npm run test:ci` plus `npm run test:e2e:mobile` with all tests passing.
- Updated `site.js` so desktop keeps navigation open by default, inline login placeholders are `E-mail` / `Password`, submit label is `Login`, and a runtime `Register` link is appended to the guest inline form without changing generated HTML.
- Reworked `styles/public.css` so the new requested header layout applies on larger screens (`title` lockup + inline login row + always-visible horizontal nav), while mobile (`<=992px`) keeps the previous hamburger/dropdown behavior.
- Added desktop/mobile-aware assertions in `tests/playwright/public-redesign.spec.js` and `tests/playwright/mobile-smoke.spec.js` so shell tests validate responsive defaults instead of one hardcoded nav state.
- Re-ran `npm.cmd run test:ci` and `npm.cmd run test:e2e:mobile`; both suites passed after the responsive desktop/mobile split.
- Logged and tracked a local environment issue where PowerShell execution policy blocks `npm.ps1`, using `npm.cmd` as the immediate workaround for command execution.
- Refined `styles/public.css` again so the public brochure keeps a light section backdrop, renders both left/right cards on the same dark card treatment, uses one gold text color across the visible public content, and visually merges the desktop login strip with the nav into one connected shell block.
- Re-ran `npm.cmd run test:ci` and the focused public Playwright regression (`tests/playwright/public-redesign.spec.js`) after the shell/background merge; both stayed green.
- Adjusted `styles/public.css` so the homepage now keeps a light backdrop behind the box layout while forcing all homepage content boxes and the CTA/quote panel onto the same dark card treatment.
- Reworked the desktop header shell to make `login + menu` one combined module with no horizontal divider between them, while matching the shared module height to the `title.png` board and preserving the older mobile dropdown behavior.
- Re-ran `npm.cmd run test:ci` and the focused public Playwright regression (`tests/playwright/public-redesign.spec.js`) after the homepage backdrop and combined-shell pass; both stayed green.
- Added the new root-level `mainbackground.png` asset and switched the global site background in `styles/base.css` to use it as the main page backdrop under the existing dark/gold overlays.
- Updated `styles/public.css` so homepage sections become transparent over the shared backdrop instead of repainting it, and enlarged/repositioned the desktop nav text upward within the combined `login + menu` shell while keeping mobile behavior unchanged.
- Re-ran `npm.cmd run test:ci` and the focused public Playwright regression (`tests/playwright/public-redesign.spec.js`) after the main-background and desktop menu alignment pass; both stayed green.
- Updated `styles/public.css` again so the sticky public header also renders over `mainbackground.png`, using a darker overlay to keep the `title + login + menu` shell readable while visually continuing the same backdrop behind the top of the page.
- Re-ran `npm.cmd run test:ci` and the focused public Playwright regression (`tests/playwright/public-redesign.spec.js`) after the header-background pass; both stayed green.
- Added the new root-level `boxbackground.png` asset and switched the shared card surfaces in `styles/base.css` to render over that texture, leaving the header outside this change.
- Updated `styles/public.css` so public detail cards, homepage cards and the quote panel use the same `boxbackground.png` treatment instead of separate flat dark fills.
- Re-ran `npm.cmd run test:ci` and the focused public Playwright regression (`tests/playwright/public-redesign.spec.js`) after the box-background rollout; both stayed green.
- Tuned `styles/tokens.css` to a warmer, slightly deeper gold palette so the site typography reads more naturally against `boxbackground.png` instead of the earlier paler yellow.
- Updated `styles/base.css` placeholder color plus `styles/public.css` card typography styling so non-header boxes now use a subtle dark text-shadow over `boxbackground.png`, improving legibility without flattening the marble texture.
- Re-ran `npm.cmd run test:ci` and the focused public Playwright regression (`tests/playwright/public-redesign.spec.js`) after the typography/color pass; both stayed green.
- Updated `styles/public.css` so the public menu buttons now animate with a light `scale(1.2)` hover/focus zoom and return to normal size when the cursor leaves, using CSS transitions only.
- Re-ran `npm.cmd run test:ci` and the focused public Playwright regression (`tests/playwright/public-redesign.spec.js`) after the menu-hover pass; both stayed green.
- Updated `scripts/publicPageRenderer.js` and `styles/public.css` so each public nav link now wraps its label in a `span`, allowing only the text to scale on hover/focus while the `|` separators remain fixed.
- Regenerated the service/location public pages plus refreshed the title asset manifest outputs so the committed HTML stays in sync with the updated public-nav markup and current optimized title dimensions.
- Re-ran `npm.cmd run test:ci` and the focused public Playwright regression (`tests/playwright/public-redesign.spec.js`) after the menu-text-hover refinement; both stayed green.
- Updated `styles/base.css` and `styles/public.css` so `boxbackground.png` now stretches with each rendered box instead of being cropped with `cover`, and the image's own gold frame can read as the visible box border.
- Removed the extra public card inset border overlay so the `boxbackground.png` frame is not visually competing with a second synthetic border.
- Re-ran `npm.cmd run test:ci` and the focused public Playwright regression (`tests/playwright/public-redesign.spec.js`) after the box-frame pass; both stayed green.
- Updated `styles/public.css` again to remove `mainbackground.png` from the sticky header and restore the header's own dark background, keeping the global background image only behind the rest of the page.
- Re-ran `npm.cmd run test:ci` and the focused public Playwright regression (`tests/playwright/public-redesign.spec.js`) after the header-background revert; both stayed green.
- Reworked `index.html` and homepage-specific rules in `styles/public.css` so the homepage now uses the target board composition: left stacked `Coverage` / `Services`, one dominant main card on the right, and a full-width quote panel directly underneath.
- Synced the homepage nav markup with the newer public-shell structure by wrapping the menu labels in `span` elements so the text-only hover scaling also works on `/index.html`.
- Audited current duplication/performance hotspots and identified two immediately worthwhile targets: repeated helper logic across the public-page generators, and wasted work in `scripts/optimize-assets.js` when outputs are already current.
- Added `scripts/publicPageBuild.shared.js` and moved shared page-generator helpers (`buildLinksByLabel`, `buildAreaItems`, `buildFaqJsonLd`) out of `scripts/generate-service-pages.js` / `scripts/generate-location-pages.js`, reducing duplication while keeping the output contract unchanged.
- Refactored `scripts/optimize-assets.js` to cache source metadata, skip already-fresh output variants unless `FORCE_ASSET_OPTIMIZE` is set, and avoid rewriting `asset-manifest.js` when its content is unchanged.
- Updated `app.js` static asset cache headers so HTML plus all frequently changed frontend assets (`CSS`, `JS`, `png/jpg/webp/avif/svg`) now use `Cache-Control: no-cache`, while fonts keep long immutable caching.
- Added an API regression in `tests/api-v2/app-legacy-routes.test.js` to lock in the new revalidation behavior for `/styles/base.css` and `/mainbackground.png`.
- Re-ran `npm.cmd run test:ci` after the cache-header change so future deploys should show frontend updates on normal refresh without manual browser-history clearing.
- Updated `gallery.html` and `gallery.js` so the public gallery now presents itself fully as a service-led experience: SEO copy, headings, sidebar defaults, status text and service descriptions now frame the gallery around bathrooms, kitchens, interiors and exteriors instead of project case studies.
- Switched the shared public card surfaces from `boxbackground.png` to `mainbackground.png`, keeping the header shell on its separate dark background while making the main background image visible inside all content cards.
- Consolidated `Project_todos.md` into one shorter active backlog with `Now`, `Next Engineering`, `Product And Workflow`, `Tooling And Quality` and `Later` sections, removing obsolete per-deploy duplicates and moving completed-history responsibility fully into `Project_Dev_plan.md`.
- Expanded the homepage `Coverage` card to a two-column area list with `Sheffield` and `Leeds`, widened the main homepage copy so it can use the full card width, and synced the same area list into shared public-page data plus homepage structured data.
- Added `utils/pagination.js` as one shared source for offset pagination, cursor parsing and pagination DTO shaping, then rewired `routes/inbox.js`, `routes/group.js`, `routes/notifications.js` and `routes/manager.js` to use it so duplicate pagination/cursor code stops drifting across route modules.
- Added `dashboard-shared.js` and moved the shared workspace helpers (`createOverviewEntry`, `renderMailboxPreviewList`, `syncKeyedList`, `createMutedNode`, `createThreadCard`, `createMessageCard`) out of both dashboard scripts, then updated `client-dashboard.html` / `manager-dashboard.html` to load the shared module first.
- Reworked the manager workspace flow so the top of `manager-dashboard.html` now starts with an area chooser (`Projects`, `Materials / Stock`, `Services`) and then shows action buttons for the selected route (`Create`, `Browse`, `Edit`), while hiding unrelated sections until that domain is chosen.
- Added a Playwright regression in `tests/playwright/mobile-smoke.spec.js` that verifies the manager workflow chooser switches between `Projects`, `Materials / Stock` and `Services`, updates the visible actions, and hides unrelated manager sections across desktop and mobile browser projects.
- Extracted the `projects` domain from `manager-dashboard.js` into the new `manager-dashboard.projects.js` helper, moving project-list rendering, project detail caching/loading, project editor filling, project media handling, project create/edit/delete submissions, and project filter/pagination event wiring behind one controller.
- Updated `manager-dashboard.html` to load `manager-dashboard.projects.js` before the main manager dashboard script, then rewired `manager-dashboard.js` to consume the new controller via thin proxies so the rest of the workspace can keep its current contracts while the monolith gets smaller.
- Re-ran `node --check` for `manager-dashboard.js`, `manager-dashboard.projects.js` and `client-dashboard.js`, then re-ran the full manager/mobile Playwright smoke suite and `npm.cmd run test:ci`; all passed after the projects-domain extraction.
- Updated `scripts/publicPageRenderer.js` so generated service/location pages now wrap their hero copy in the same dark content-card system as the rest of the site instead of dropping gold text directly onto the bright marble background.
- Added shared hero-card styling in `styles/public.css` for `.public-hero--inner` / `.inner-hero-shell`, keeping service/location hero text on a readable dark field while still allowing the page-level image/background language to show through.
- Regenerated all public service/location pages and extended `tests/playwright/public-redesign.spec.js` to assert the generated service hero now renders as a dark card, then re-ran `npm.cmd run generate:public-pages`, `npm.cmd run test:ci`, and the focused public Playwright suite successfully.
- Routed the `Discuss wall systems` CTA on `services.html` into `/quote.html?projectType=interior#quote-card`, added the `quote-card` anchor to the quote page, and taught `site.js` to preselect the quote project-type field from the `projectType` query param while still honoring explicit `data-selected-value`.
- Updated the browser `brand.js` service hrefs plus `scripts/publicPages.shared.js` service links so interior/external wall-system footer entries also preserve quote context, then regenerated the public service/location pages to keep those footer links in sync.
- Added a public Playwright regression that clicks `Discuss wall systems` from the services hub and verifies the quote page opens at the quote card with `Interior Wall Systems` preselected; also updated `quote.js` to restore the contextual project type after a successful form reset.
- Reworked the top of `services.html` so the page now starts with an explicit service list (`Bathrooms`, `Kitchens`, `Interior Renovations`, `Large-format Tiling`, `Carpentry`, `Wall Systems`, `Flooring Installation`) and places the explanatory copy underneath instead of leading with abstract hub wording.
- Added `service-summary-list` / `service-summary-item` styling in `styles/public.css` so the service list reads as a clear two-column desktop summary and collapses to one column on smaller screens; updated the public Playwright services-page assertion accordingly.
- Corrected that `services.html` top-section change by removing the added service-summary strip completely, so the intro card is back to the intended `Services` eyebrow + heading + lead copy, with the real service listing staying only in the lower service cards section.
- Removed the now-unused `service-summary-list` / `service-summary-item` rules from `styles/public.css` and updated the public Playwright services-page regression to assert the page heading plus existing lower service cards instead of the removed top strip.
- Re-split the brochure background system so `mainbackground.png` remains the background language for cards while `boxbackground.png` is now used for form fields and smaller boxed UI surfaces (`input`, `select`, `textarea`, gallery stage/chips/lightbox panels, FAQ-style nested boxes) instead of leaving legacy flat dark fills in place.
- Removed the remaining service/location hero-card image override from `.public-hero--inner .inner-hero-shell`, so generated hero cards now also follow the same `mainbackground` card treatment instead of letting the old hero image hide it.
- Added a new `migrations/202603170001-group-thread-project-management.js` migration plus the matching `GroupThread` / `models/index.js` updates so project chat threads can now belong to a `projectId`, inherit project/quote relationships and backfill from existing quote-linked project chats.
- Expanded `routes/group.js` into a fuller project-chat backend: serialized thread summaries now include creator/project/quote/member data, staff can create a project chat from a project, project members can be auto-seeded from the client/assigned staff, and thread admins can remove members while preserving at least one admin.
- Updated `routes/manager/project-routes.js` and `routes/manager.js` so project create/update flows sync existing quote-linked chat threads onto the real project record as soon as that project exists, keeping group chat history portable across the quote-to-project transition.
- Rebuilt the messaging area in `manager-dashboard.html` and `manager-dashboard.js` so managers can create private threads by email lookup, create project chats from a selected project, seed/add/remove participants, and review project-chat members directly inside the workspace instead of only reading existing thread lists.
- Wired the project-chat chooser into the extracted `manager-dashboard.projects.js` controller so project options stay in sync with project CRUD changes and the messaging UI no longer depends on stale project lists.
- Added `tests/api-v2/group-thread-management.test.js` to lock in project-chat creation plus admin member removal, fixed the stub layer to respect `Op.in`, UUID route validation and member `destroy()` behavior, and kept the new API coverage green.
- Added a focused Playwright regression in `tests/playwright/mobile-smoke.spec.js` that drives the new manager messenger flow end-to-end across desktop Chromium, mobile Chromium and mobile WebKit: create direct thread, create project chat, add participant and remove participant.
- Re-ran `node --check` on the touched route/dashboard/test files, re-ran `node --test tests/api-v2/group-thread-management.test.js`, re-ran the focused Playwright manager messenger test, and re-ran `npm.cmd run test:ci`; all passed after the messenger-system rollout.
- Extracted the `quotes` domain from `manager-dashboard.js` into the new `manager-dashboard.quotes.js` controller, moving quote list rendering, status/priority update actions, accept flow, filter state, pagination and role-gated lazy loading behind one dedicated module.
- Updated `manager-dashboard.html` to load `manager-dashboard.quotes.js` before the main manager dashboard runtime, then rewired `manager-dashboard.js` to call the new controller for quote lazy-loading and event binding instead of keeping quote-specific UI/event code in the monolith.
- Added a focused Playwright regression that opens the manager quote section, accepts a pending quote, then saves a changed status/priority so the extracted quote controller stays covered across desktop/mobile browser projects.
- Extracted the `estimates` domain from `manager-dashboard.js` into the new `manager-dashboard.estimates.js` controller, moving estimate list rendering, detail loading, line-item rendering, create/update/delete flows, line-mode switching and lazy loading behind one dedicated module.
- Updated `manager-dashboard.html` to load `manager-dashboard.estimates.js` before the main manager dashboard runtime, then rewired `manager-dashboard.js` to call the new controller for estimate lazy-loading and event binding instead of keeping estimate-specific UI/event code inline.
- Added a focused Playwright regression that opens the manager estimates section, updates an estimate title/status and adds a custom line item so the extracted estimate controller stays covered across desktop/mobile browser projects.
- Standardised the public service naming on the brochure layer to the requested five-line set: `Full Bathroom Renovations`, `Kitchen Installation and Refurbishment`, `Tiling incl. Large Format / Wet Showers / Exterior`, `Carpentry`, and `Interior and Exterior Wall`.
- Updated `services.html`, `brand.js` and `scripts/publicPages.shared.js` so the services hub, footer service links, shared quote project-type options and generated-page footer links all use the same new service language instead of the older mixed set with separate wall/flooring labels.
- Regenerated the public service/location pages and updated the public Playwright services-page assertion so the new service labels stay locked in across desktop/mobile brochure rendering.
- Rebuilt the top of `gallery.html` into one dominant gallery card: static `Gallery` title, active-service heading/meta, a dedicated horizontal service-switch row, and the image stage below it instead of the older two-column sidebar rail.
- Updated `gallery.js` to curate the gallery into five service-led groups from the existing folder-based collections, so the switcher now matches the current public services language while keeping fullscreen image browsing and keyboard navigation intact.
- Added gallery-specific public regression coverage and refreshed `styles/public.css` so the service switcher stays readable as a horizontal button row on desktop and stacks cleanly on mobile.
- Updated the gallery Playwright regression to follow the rebuilt gallery contract by asserting the active service title/meta/status instead of the removed image-title node from the older layout.
- Extracted the remaining `messages` domain from `manager-dashboard.js` into the new `manager-dashboard.messages.js` controller, moving private inbox rendering, project-chat rendering, participant management, message sending and lazy mailbox loading behind one dedicated module.
- Updated `manager-dashboard.html` to load `manager-dashboard.messages.js` before the main manager runtime, then rewired `manager-dashboard.js` so mailbox bootstrap, lazy section loading and project-chat project-option syncing all go through the new controller instead of the monolith.
- Re-applied the card background system so the named brochure/account cards (`Home`, `About`, `Services`, `Gallery`, `Quote`, `Contact`, `Account`) all explicitly use `mainbackground.png`, while `boxbackground.png` stays reserved for smaller field/input surfaces.
- Re-ran `node --check` for the new messaging controller plus `manager-dashboard.js`, re-ran the focused Playwright manager messenger flow, re-ran the public brochure Playwright suite, and re-ran `npm.cmd run test:ci`; all passed after the controller extraction and card-background cleanup.
- Hardened the public header session-state rendering in `site.js` by adding an explicit `data-auth-view` state on the sticky shell, so authenticated brochure pages cannot render the guest inline login form at the same time as the session/logout controls.
- Added CSS guard rails in `styles/public.css` for `guest/session/hidden` header states and styled browser autofill in `styles/base.css` so Chrome no longer paints public-shell login fields white against the dark brand shell.
- Extended `tests/playwright/public-redesign.spec.js` so authenticated state is now checked on both the homepage and a generated service page, covering the regression where service pages could still show the inline email/password inputs while logged in.
- Reordered `quote.html` so the practical quote row (`Before you send` + form) now appears above the descriptive row (`Quote` + `How to brief`), while keeping `#quote-card` attached to the form section for CTA deep-linking.
- Added a Playwright assertion on `/quote.html` that locks in this section order by checking the `#quote-card` block renders above the main quote-intro heading.
- Extracted the `services` and `materials` catalog domain from `manager-dashboard.js` into the new `manager-dashboard.catalog.js` controller, moving catalog list rendering, filter/pagination state, create flows, update/delete actions and seed refresh handling behind one dedicated module.
- Updated `manager-dashboard.html` to load `manager-dashboard.catalog.js` before the main manager runtime, then rewired `manager-dashboard.js` so lazy section loading, estimate preloading and catalog refresh after seed all go through the new controller instead of the monolith.
- Added a focused Playwright regression that updates a service and a material from the manager dashboard to keep the extracted catalog controller covered across desktop/mobile browser projects.
- Extracted the `clients` and `staff` domain from `manager-dashboard.js` into the new `manager-dashboard.people.js` controller, moving people-list rendering, people search filters, lazy loading and staff-create flow behind one dedicated module.
- Updated `manager-dashboard.html` to load `manager-dashboard.people.js` before the main manager runtime, then rewired `manager-dashboard.js` so clients/staff lazy loading and event binding now go through the new controller instead of the monolith.
- Added a focused Playwright regression that loads manager `Clients` / `Staff` sections and creates a new staff member, keeping the extracted people controller covered across browser projects.
- Tightened `app.js` static cache headers for active iteration so HTML plus frequently changed frontend assets (`CSS`, `JS`, `png/jpg/webp/avif/svg`) now return `Cache-Control: no-store`, removing the need to clear browser history/cache after each deploy while this UI-heavy phase is in progress.
- Updated `tests/api-v2/app-legacy-routes.test.js` to lock in the stronger `no-store` behavior for `/index.html`, `/styles/base.css` and `/mainbackground.png`.
- Added the missing gallery stage-card CSS in `styles/public.css`: the side previews are now positioned as absolute roller cards inside a perspective stage, with stable aspect ratios, visible frames, contained images and caption overlays instead of rendering as overlapping static-flow blocks.
- Increased the gallery stage minimum height and padding so left/right preview cards have room to render cleanly on desktop while still collapsing to a tighter single-card layout on mobile.
- Re-ran the focused public Playwright regression and `npm.cmd run test:ci`, then spot-checked the local gallery in a real browser against a temporary static server after the side-preview fix; the gallery contract stayed green.
- Added `manager-dashboard.shell.js` and moved the remaining manager-shell concerns out of `manager-dashboard.js`: workflow chooser rendering, company-events/mailbox/available-options overview, email autocomplete lookup, lazy-section bootstrapping, seed handling, logout flow and initial session bootstrap now live in one dedicated shell controller.
- Updated `manager-dashboard.html` to load `manager-dashboard.shell.js` before the main manager runtime, then rewired `manager-dashboard.js` to instantiate feature controllers plus the shell controller and act as a thin orchestration layer instead of keeping shell/bootstrap logic inline.
- Reduced `manager-dashboard.js` from 1161 lines to 606 lines after the shell extraction, leaving it as a much smaller coordinator over the extracted `projects`, `quotes`, `catalog`, `people`, `estimates` and `messages` modules.
- Re-ran `node --check` for `manager-dashboard.shell.js` and `manager-dashboard.js`, ran focused manager Playwright coverage for workflow chooser and messenger flows, and re-ran `npm.cmd run test:ci`; all stayed green after the shell refactor.
- Added `client-dashboard.projects.js` and moved the client project/document-upload slice out of `client-dashboard.js`: project-card rendering, upload project select syncing and the client documents upload submit flow now sit behind a dedicated controller.
- Added `client-dashboard.overview.js` and moved the overview slice out of `client-dashboard.js`: metrics, quote/service lists, project-status summary, mailbox summary, available options and overview loading now sit behind a dedicated controller.
- Updated `client-dashboard.html` to load the new client overview/projects controllers before the main runtime, then rewired `client-dashboard.js` to instantiate both controllers and keep only the messaging/direct-thread shell logic inline for the next extraction pass.
- Re-ran `node --check` for `client-dashboard.projects.js`, `client-dashboard.overview.js` and `client-dashboard.js`, ran focused Playwright coverage for the client mobile dashboard flows, and re-ran `npm.cmd run test:ci`; all stayed green after the first client-dashboard modularization pass.
- Added `client-dashboard.messages.js` and moved the remaining client communication slice out of `client-dashboard.js`: direct-manager thread summaries, project-chat thread summaries, message rendering, lazy mailbox loading and both message-send flows now sit behind a dedicated controller.
- Updated `client-dashboard.html` to load the new client messages controller before the main runtime, then rewired `client-dashboard.js` so it now coordinates overview, projects/documents and messaging controllers instead of carrying direct/project chat logic inline.
- Tightened the direct-thread refresh path while extracting the client messenger slice, so creating a new private thread now forces a thread-summary refresh and selects the newly created thread instead of falling back to stale state.
- Added a focused Playwright regression for the client workspace that opens a direct-manager thread and a project-chat thread and asserts the correct message history loads for both routes across desktop/mobile browser projects.
- Re-ran `node --check` for `client-dashboard.messages.js`, `client-dashboard.js` and the touched Playwright suite, re-ran focused client messaging coverage, and re-ran `npm.cmd run test:ci`; all stayed green after the client messenger extraction.
- Added `client-dashboard.shell.js` and moved the remaining client shell concerns out of `client-dashboard.js`: session bootstrap, stored-user recovery, role guard, login redirect path and logout flow now sit behind one dedicated shell controller.
- Updated `client-dashboard.html` to load the new client shell controller before the main runtime, then rewired `client-dashboard.js` so it now just instantiates `overview`, `projects`, `messages` and `shell` controllers instead of holding bootstrap/logout logic inline.
- Reduced `client-dashboard.js` from 343 lines to 279 lines after the shell extraction, leaving it as a thin orchestration layer over the extracted client workspace modules.
- Re-ran `node --check` for `client-dashboard.shell.js` and `client-dashboard.js`, re-ran focused client Playwright coverage for mobile menu plus direct/project chat loading, and re-ran `npm.cmd run test:ci`; all stayed green after the final client-shell extraction.
- Added `utils/assetVersioning.js` with shared helpers that append a startup-scoped `?v=` token to local CSS/JS/image asset URLs inside served HTML, plus middleware for dynamically versioning brochure/dashboard HTML responses.
- Updated `app.js` to use the new versioned-HTML middleware before the static file server, while keeping the current `no-store` cache policy for iterated frontend assets and exposing the generated asset version through `app.locals`.
- Updated `scripts/playwright-static-server.js` to use the same versioned-HTML middleware as the real app, so local smoke tests and browser-debug runs behave the same way as the deployed site.
- Extended `tests/api-v2/app-legacy-routes.test.js` to assert that `/index.html` now contains versioned CSS, JS and title-image URLs, then re-ran `npm.cmd run test:ci` plus a focused Playwright smoke run to keep both app and local static server behavior green.
- Moved dashboard-only layout helpers (`dashboard-search-row`, `dashboard-inline-check`, `dashboard-list`, `dashboard-grid`, `workspace-contact-list`) out of `styles/base.css` and into `styles/workspace.css`, so workspace structure no longer depends on the shared base layer for its grid/list primitives.
- Moved public-only action/link helpers (`page-actions`, `contact-link-list`, `home-card-contact-list-link`) out of `styles/base.css` and into `styles/public.css`, keeping brochure CTA stacks and contact-link behavior owned by the public stylesheet instead of the shared base layer.
- Kept genuinely shared primitives in `styles/base.css` on purpose, especially the card foundations, footer structure and generic typography/form rules, so the cleanup improves ownership without introducing duplicated footer/card systems across public and workspace surfaces.
- Updated `tests/playwright/public-redesign.spec.js` and `tests/playwright/mobile-smoke.spec.js` to accept the versioned `title.png?v=...` header asset URLs introduced by the HTML cache-busting middleware, then re-ran the focused public suite, the full mobile-smoke suite and `npm.cmd run test:ci`; all passed after the CSS ownership cleanup.
- Hardened `scripts/run-playwright.js` against `EADDRINUSE` by adding a `findAvailablePort()` probe that starts from the preferred `PW_STATIC_PORT`, scans forward for a free port and exports the chosen port through `PW_STATIC_PORT` / `PW_BASE_URL` before Playwright boots.
- Updated `tests/playwright/playwright.config.js` to consume `PW_STATIC_PORT` / `PW_BASE_URL` instead of hard-coding `127.0.0.1:4173`, so external and internal static-server flows follow the same selected port and no longer assume a single global local port.
- Added `tests/api-v2/run-playwright-port-selection.test.js` to lock in the occupied-port fallback behavior, then re-ran `node --check scripts/run-playwright.js`, the focused brochure Playwright smoke, the direct port-selection test and `npm.cmd run test:ci`; all passed.
- Normalised the remaining public card surfaces in `styles/public.css` so large brochure/gallery cards use `mainbackground.png` consistently: gallery stage, roller side cards, gallery lightbox panels, media preview cards and FAQ cards now follow the same card-background language instead of mixing in `boxbackground.png`.
- Kept `boxbackground.png` intentionally reserved for smaller controls and field-like surfaces (inputs, selects, textareas, gallery service buttons, arrows and other compact UI), preserving the original split of `mainbackground = cards` and `boxbackground = smaller panels/controls`.
- Extended `tests/playwright/public-redesign.spec.js` with a gallery-stage background assertion and re-ran the focused public Playwright suite plus `npm.cmd run test:ci`; all stayed green after the public-card background cleanup.
- Tightened the desktop gallery stage so side-preview roller cards size down relative to the viewport, gain more side padding inside the stage and compute their horizontal travel from the real stage/card width instead of a fixed translate, which stops the side previews from crowding the centered image.
- Extracted the Playwright port-probing logic into `scripts/playwright-port.js`, then updated both `scripts/run-playwright.js` and `scripts/playwright-static-server.js` to share it so standalone local preview servers also fall forward to a free port instead of failing on the first occupied one.
- Extended `tests/api-v2/run-playwright-port-selection.test.js` so it now covers both the shared `findAvailablePort()` helper and `startStaticServer()` itself when the preferred port is already occupied.
- Re-ran `node --check` for the new/shared Playwright port helpers, re-ran `node --test tests/api-v2/run-playwright-port-selection.test.js`, re-ran the focused brochure Playwright suite through `--grep`, and spot-checked the rebuilt gallery locally in a real browser on the new auto-selected static-server port; the gallery side previews stayed centered and readable without overlapping the main image.
- Polished the desktop gallery stage further by moving photo arrows onto the stage as overlay controls, centering the service-switch row, and keeping long service labels on one line on desktop while still allowing them to wrap back on mobile.
- Re-ran the focused public gallery/brochure Playwright suite and verified the updated gallery stage visually in a local browser preview after the overlay-arrow pass; the centered image stayed dominant while the side previews and controls remained readable.
- Removed the extra alternating section-band backgrounds from `page-gallery` so the gallery page now uses one continuous page background and relies on the cards themselves, rather than dark/light striping behind the sections.
- Re-ran the focused public brochure/gallery Playwright suite after the gallery-background cleanup; the route contract stayed green.
- Added `utils/threadSummaries.js` and moved reusable mailbox-summary logic into shared helpers, so direct inbox and project-chat routes can both expose latest-message preview data without duplicating summary code.
- Updated `routes/inbox.js` to return direct-thread summaries with `latestMessagePreview`, `latestMessageAt`, `latestMessageSenderId` and `unreadCount`, and added a new `POST /api/inbox/threads/:id/read` endpoint that marks all unread messages in the selected direct thread as read in one call.
- Updated `routes/group.js` to return richer project-chat summaries with `latestMessagePreview`, `latestMessageAt`, `latestMessageSender` and `messageCount` on thread-list responses and new-thread creation responses.
- Reworked the shared workspace thread-card UI in `dashboard-shared.js` and `styles/workspace.css` so manager/client mailbox lists now render a proper thread title row, preview copy, meta row and unread badge instead of the older two-line summary block.
- Updated `manager-dashboard.messages.js`, `manager-dashboard.shell.js`, `client-dashboard.messages.js` and `client-dashboard.overview.js` so both dashboards now surface latest-message previews in thread lists and mailbox previews, and direct-thread unread state clears automatically when a thread is opened.
- Tightened the client direct/project send flows so thread summaries refresh immediately after sending, keeping the preview/meta lines in sync with the latest message instead of leaving stale mailbox summaries on screen.
- Added API regressions in `tests/api-v2/inbox-thread-summaries.test.js` and `tests/api-v2/group-thread-summaries.test.js`, then updated the Playwright client mailbox mock in `tests/playwright/mobile-smoke.spec.js` so the richer message summaries and auto-read flow stay covered across desktop/mobile browser projects.
- Re-ran `node --check` on the touched summary/message modules, re-ran the new summary API tests, re-ran the focused mailbox Playwright suite, and re-ran `npm.cmd run test:ci`; all passed after the messaging-system upgrade.
- Extended the messaging UI end-to-end with attachment support across both dashboards: `manager-dashboard.html`, `client-dashboard.html`, `manager-dashboard.messages.js`, `client-dashboard.messages.js`, `dashboard-shared.js` and `styles/workspace.css` now support selecting files, sending multipart message uploads, and rendering attachment links directly inside message history cards.
- Updated `routes/inbox.js` so direct threads can be opened in `createOnly` mode without forcing an opening text message, which lets a brand-new private conversation start from an attachment upload instead of requiring a dummy text message first.
- Added a new inbox API regression for `createOnly` thread creation in `tests/api-v2/inbox-thread-summaries.test.js`, plus a shared text fixture and new Playwright coverage in `tests/playwright/mobile-smoke.spec.js` that exercises attachment sending in direct and project chat for both client and manager dashboards.
- Fixed a real local tooling bug in `scripts/playwright-port.js` and `tests/api-v2/run-playwright-port-selection.test.js`: the port probe now checks the actual listen surface instead of only `127.0.0.1`, so local Playwright/static-server runs stop falsely claiming `4173` is free when another process already holds the port on a broader interface binding.
- Re-ran `node --check` on all touched dashboard/message files, re-ran the inbox summary API tests, re-ran the Playwright port-selection API test, re-ran focused Playwright attachment flows for both dashboards, and re-ran `npm.cmd run test:ci`; all passed after the attachment upgrade.
- Synced the hard-coded footer service fallbacks in `auth.html`, `client-dashboard.html` and `manager-dashboard.html` with the canonical five-service set already defined in `brand.js`, removing the stale `External Wall Systems`, `Interior Wall Systems` and `Flooring Installation` links from workspace/account HTML fallback markup.
- Added shared Playwright footer assertions in `tests/playwright/mobile-smoke.spec.js` so `auth`, `client-dashboard` and `manager-dashboard` all stay aligned with the same five service labels and cannot regress back to the older wall/flooring split.
- Refined `gallery.html` on narrower desktop/tablet widths by collapsing the intro grid to a single column earlier and hiding the side preview cards before they become cramped, leaving one dominant center image in the stage while keeping the wide-desktop multi-card gallery intact.
- Added a focused Playwright regression in `tests/playwright/public-redesign.spec.js` that forces a 900px-wide desktop viewport, checks the gallery intro has collapsed to one column, verifies the center card is still visible, and locks in the hidden left/right preview state on that narrower desktop breakpoint.
- Added a small top offset to `.public-page-main` so brochure pages breathe slightly below the sticky shell, then aligned the gallery intro grid to `start` so its two intro cards size to their own content instead of stretching to the same height and leaving a large dead dark block on the right-hand card.
- Re-ran focused brochure/gallery Playwright coverage and the full `npm.cmd run test:ci` suite after the gallery-intro spacing cleanup; both stayed green.
- Tuned the gallery roller card presentation so side previews now use thumbnail-style `object-fit: cover` while the center image keeps `object-fit: contain`, improving the usefulness of desktop side previews without sacrificing the readability of the active image.
- Extended `tests/playwright/public-redesign.spec.js` with a gallery image-fit regression that locks in `contain` for the center card and `cover` for the visible side cards on wide desktop viewports.
- Ran a Sonar cleanup pass over dashboard/browser modules and shared helpers: switched browser globals from `window` to `globalThis`, updated `scripts/playwright-port.js`, `scripts/playwright-static-server.js` and `utils/assetVersioning.js` to modern `node:` imports/helpers, changed `routes/group.js` to `Object.hasOwn(...)` plus `.at(-1)`, and simplified a few direct optional checks in the client shell/projects flow.
- Removed a couple of noisy nested preview ternaries by introducing small preview helpers in the client overview/messages and manager shell/messages controllers, keeping the UI text the same while making the summary cards easier to maintain.
- Re-ran `node --check` across all touched dashboard/site/util files, re-ran `npm.cmd run test:ci`, re-ran the focused public brochure/gallery Playwright suite, and re-ran the focused workspace/mobile-smoke Playwright suite; all stayed green after the Sonar portability/readability pass.
- Re-checked the gallery locally with fresh brochure screenshots, then kept the center gallery card on `object-fit: contain` while switching the visible side previews to `object-fit: cover`, so wide-desktop thumbnails read like real previews instead of padded dark panels.
- Re-ran the focused brochure/gallery Playwright suite and the full `npm.cmd run test:ci` package after the thumbnail-fit polish; both stayed green.
- Continued the heavier Sonar cleanup in `client-dashboard.messages.js` by extracting thread-open callbacks, direct-thread empty-state text, reusable message-card rendering, lazy-section task builders and both mailbox submit flows into dedicated helpers instead of keeping the whole client messenger flow nested inline.
- Kept the direct-message behavior unchanged while making thread creation clearer: new helper functions now build the direct-thread payload, create a new direct thread when needed, reuse the existing thread when present, and route both group/direct send paths through smaller submit helpers.
- Re-ran `node --check client-dashboard.messages.js`, a focused Playwright mailbox regression for client direct/project chat loading plus attachment sending, and `npm.cmd run test:ci`; all stayed green after the client mailbox Sonar refactor.
- Continued the heavier Sonar cleanup in `manager-dashboard.messages.js` by extracting direct/group thread card rendering, shared message-card rendering, group-member sorting/render helpers and the private/project thread submit flows into dedicated functions instead of keeping the manager messenger as one large callback-heavy module.
- Added a small managed-submit wrapper for manager messaging forms so submit buttons and regular form submission behave consistently on desktop/mobile, which removed the flaky `mobile-webkit` failure in the project-chat creation flow without changing the manager UX.
- Re-ran `node --check manager-dashboard.messages.js`, the focused manager Playwright regressions for private/project attachments plus thread creation and participant management, and `npm.cmd run test:ci`; all stayed green after the manager mailbox Sonar refactor.
- Continued the heavier Sonar cleanup in `manager-dashboard.shell.js` by extracting helpers for manager-domain workflow buttons, company-event building, available-option cards, live email autocomplete, lazy-section setup, manager-session bootstrap, seed refresh and logout handling, leaving the shell controller as a cleaner orchestration layer instead of one large callback-heavy module.
- Re-ran `node --check manager-dashboard.shell.js` and the focused manager Playwright regressions for workflow chooser plus project-chat participant management; after one transient `mobile-webkit` retry the full focused manager suite passed cleanly again, confirming the shell refactor kept the runtime behavior intact.
- Continued the heavier Sonar cleanup in `gallery.js` by extracting helpers for curated service collections, gallery status/meta text, roller-card creation/update and gallery init/bootstrap flow, which keeps the service-led gallery behavior intact while reducing nested callbacks and nested template assembly in the public UI controller.
- Re-ran `node --check gallery.js`, re-ran the focused public gallery Playwright regressions for core brochure routing, narrower-desktop gallery collapse and center/side image-fit behavior, and kept that full focused gallery suite green after the refactor.
- Continued the heavier Sonar cleanup in `manager-dashboard.catalog.js` by extracting shared catalog loaders, generic catalog render helpers, service/material card factories, payload builders, save/delete handlers and paginated reload actions, which removes a large amount of duplicated inline logic from the manager services/materials catalog flow without changing the API contract or dashboard behavior.
- Re-ran `node --check manager-dashboard.catalog.js`, re-ran the focused manager Playwright regressions for workflow chooser plus services/materials catalog updates, and re-ran `npm.cmd run test:ci`; all stayed green after the catalog refactor.
- Continued the heavier Sonar cleanup in `manager-dashboard.estimates.js` by extracting estimate-card and line-item render helpers, reusable GBP formatting, create/update payload helpers, line validation and submit/delete handlers, which keeps the manager estimate workflow intact while removing more nested inline logic from create/update/delete and line-item flows.
- Re-ran `node --check manager-dashboard.estimates.js`, re-ran the focused manager Playwright regressions for workflow chooser plus estimate create/update/custom-line flows, and re-ran `npm.cmd run test:ci`; all stayed green after the estimates refactor.
- Continued the heavier Sonar cleanup in `routes/group.js` by extracting reusable request-validation, membership/admin guard, project-chat seed-member and thread-creation helpers, which keeps group-thread creation/message/member routes on the same contract while lowering route complexity and duplicated access-control checks.
- Re-ran `node --check routes/group.js`, re-ran the focused `group-thread-management` and `group-thread-summaries` API tests, and re-ran `npm.cmd run test:ci`; all stayed green after the group-route refactor.
- Continued the heavier Sonar cleanup in `site.js` by extracting public auth-state helpers, account/auth link syncing, inline session-copy rendering and dashboard accordion setup helpers, while switching custom `data-auth-view` updates over to `.dataset` and keeping the public/workspace shell behavior unchanged.
- Re-ran `node --check site.js`, re-ran the full public brochure Playwright suite plus focused mobile auth/dashboard shell regressions, and re-ran `npm.cmd run test:ci`; all stayed green after the site-shell refactor.
- Fixed the Windows Playwright runner issue where direct file targeting like `node scripts/run-playwright.js -c .\\tests\\playwright\\playwright.config.js .\\tests\\playwright\\public-redesign.spec.js` could fail with `No tests found` because Playwright treated backslash-heavy positional args as regex-like patterns instead of portable file paths.
- Updated `scripts/run-playwright.js` to normalize existing config/test path arguments into forward-slash project-relative paths before delegating to Playwright, while leaving non-path flags like `--grep` untouched.
- Extended `tests/api-v2/run-playwright-port-selection.test.js` with a regression that locks in the Windows-style `.\tests\...` argument normalization, then re-ran `node --check scripts/run-playwright.js`, re-ran `node --test tests/api-v2/run-playwright-port-selection.test.js`, re-ran the exact Windows-style direct-file Playwright command against `public-redesign.spec.js`, and re-ran `npm.cmd run test:ci`; all passed after the runner fix.
- Renamed the manager dashboard quick-access card to an account-style panel (`Account Panel` / `Quick Access`), updated the manager option labels to the requested set (`Create Project`, `ProjectManager`, `QuotesReview`, `ServicesManage`, `MaterialsTrack`, `Clients`, `Staff`, `Estimate`, `PrivateChat`, `ProjectChat`), and changed the materials chooser wording from `Stock` to `Storage` in the manager workflow surface.
- Added the same manager quick-access route list to the logged-in `auth.html` account panel through `auth.js`, so manager/admin/employee accounts can jump straight from account settings into the main manager dashboard sections without first landing on the full dashboard shell.
- Added focused Playwright coverage in `tests/playwright/mobile-smoke.spec.js` for the new manager account quick-access panel on `auth.html`, the renamed manager `Quick Access` labels, and the `Materials / Storage` workflow wording, then re-ran the focused auth/manager Playwright suite plus `npm.cmd run test:ci`; all stayed green after the account-panel rename and quick-access rollout.
- Switched workspace field surfaces (operations cards, overview entries, mailbox stats/columns, dashboard list items, message threads and attachment rows) to use `boxbackground.png` overlays so the highlighted panels inside cards consistently match the requested field background across manager/client surfaces.
- Re-ran `npm.cmd run test:ci`; all stayed green after the boxbackground field pass.
- Removed the remaining legacy page-band backgrounds from the brochure/workspace shell by making `public-section`, `public-section--dark`, `public-section--light` and `workspace-content-shell` transparent, then restored `mainbackground.png` as the actual card background for generic workspace `.card` surfaces so pages like `about.html`, `services.html`, `quote.html`, `contact.html`, `auth.html` and both dashboards no longer show old light/dark bands behind the cards.
- Extended `tests/playwright/public-redesign.spec.js` with a regression that locks `about.html` section backgrounds to `none`, then re-ran the focused public brochure suite plus the full `npm.cmd run test:ci` package to keep the background cleanup covered.
- Reordered `gallery.html` so the interactive `Service Rail / Gallery / active service meta` card now appears before the explanatory `Service Gallery / How To Browse` intro cards, matching the requested flow where the service switcher leads and the descriptive copy sits below it.
- Extended `tests/playwright/public-redesign.spec.js` with a gallery position regression that asserts the `#gallery` card appears above the intro grid, then re-ran the focused public brochure suite plus `npm.cmd run test:ci`; both stayed green after the gallery-order change.
- Added a real manager quick-access panel directly into the public header shell through `site.js`: after login on public pages, `employee/manager/admin` users now get an `Account Panel / Quick Access` row with the requested shortcuts (`Create Project`, `ProjectManager`, `QuotesReview`, `ServicesManage`, `MaterialsTrack`, `Clients`, `Staff`, `Estimate`, `PrivateChat`, `ProjectChat`) instead of just a plain `Account` nav link.
- Styled the new public-header quick-access panel in `styles/public.css` so it fits the existing shell on desktop and collapses cleanly into a chip grid on mobile, while leaving the standard client/public account behavior unchanged.
- Extended `tests/playwright/public-redesign.spec.js` with a logged-manager regression that asserts the header quick-access panel appears on public pages and the plain nav `Account` link is hidden in that state, then re-ran the focused public Playwright suite plus `npm.cmd run test:ci`; both stayed green after the header quick-access rollout.
- Centralised the manager quick-access definitions into `brand.js` as shared `managerQuickAccess` metadata, so `auth.js`, `site.js` and `manager-dashboard.shell.js` now consume the same labels, hrefs and role rules instead of maintaining three separate hard-coded lists.
- Kept the dashboard-specific copy where it belongs by letting `manager-dashboard.shell.js` derive `detail` and `meta` text from the shared quick-access `key`, which keeps the manager workspace informative without turning the shared brand config into a dashboard-only blob.
- Normalised manager-shell quick-access hrefs back to local `#section` anchors while keeping public/auth quick-access links on full dashboard URLs, fixing the regression where the shared config briefly broke `#manager-available-options` link expectations on `manager-dashboard.html`.
- Re-ran `node --check` for `brand.js`, `auth.js`, `site.js` and `manager-dashboard.shell.js`, re-ran `npm.cmd run test:ci`, re-ran the focused public-header/auth quick-access Playwright regressions, and re-ran the manager-dashboard quick-access Playwright regression; all stayed green after the shared quick-access deduplication pass.
- Centralised shared role metadata into `brand.js` as `roleProfiles`, so account labels (`Client`, `Employee`, `Manager`, `Admin`), default account destinations and manager-workspace flags now come from one place instead of being redefined in both `auth.js` and `site.js`.
- Updated `auth.js` and `site.js` to resolve account redirects, public/account labels and manager quick-access visibility from the shared role metadata, which keeps login/account routing portable for future native clients as well as the current web shell.
- Updated `client-dashboard.shell.js` and `manager-dashboard.shell.js` to reuse the same role destination/permission metadata for auth redirects and seed-button gating, then re-ran the focused public/auth/manager Playwright regressions plus `npm.cmd run test:ci`; all stayed green after the role-profile deduplication pass.
- Extended `package.json` with a light generated-page workflow: `npm run generate:public-pages:content` now rebuilds only the generated brochure HTML, while `npm run generate:public-pages:full` keeps the previous full asset-optimisation + HTML pipeline and `npm run generate:public-pages` remains the compatibility alias for the full pass.
- Updated `scripts/verify-generated.js` so its failure guidance points developers to the lighter HTML-only rebuild first, and updated `README.md` to document when to use `generate:public-pages:content` versus the full asset rebuild.
- Re-ran `npm.cmd run generate:public-pages:content`, `npm.cmd run verify:generated` and `npm.cmd run test:ci`; all passed, confirming the lighter workflow is ready for everyday brochure/content iteration without touching the asset optimisation pass.
- Upgraded the runtime upload middleware from `multer 1.4.5-lts.x` to `multer 2.1.1`, which removes the known direct Multer 1.x vulnerability from deploy logs without changing any existing inbox/project/client upload route contracts.
- Added a real multipart inbox upload regression to `tests/api-v2/inbox-thread-summaries.test.js`: the test now sends a real attached file through `/api/inbox/threads/:id/messages/upload`, asserts the stored attachment metadata/body fallback, and deletes the temporary uploaded file from `UPLOADS_DIR` after the assertion.
- Re-ran `npm.cmd ls multer`, `node --test tests/api-v2/inbox-thread-summaries.test.js` and `npm.cmd run test:ci`; all passed after the Multer 2.x upgrade and the new multipart regression.
- Captured the new dependency baseline after the Multer upgrade: production audit warnings dropped to `6` (`4 moderate`, `2 high`), and the remaining deprecated/vulnerable production chain is now mainly `bcrypt -> @mapbox/node-pre-gyp -> tar/glob/rimraf/npmlog`, which should be the next isolated dependency-cleanup pass.
- Upgraded `bcrypt` from `5.1.1` to `6.0.0`, which moves native packaging over to `node-gyp-build` and removes the old `@mapbox/node-pre-gyp -> tar/glob/rimraf/npmlog` production warning chain without requiring any code changes in `models/User.js`.
- Re-ran `npm.cmd ls bcrypt @mapbox/node-pre-gyp node-gyp-build --omit=dev`, `node --test tests/api-v2/auth-flow.test.js` and `npm.cmd run test:ci`; all passed after the `bcrypt 6.0.0` upgrade, so login, password verification and password-change session revocation stayed intact.
- Captured the next dependency baseline after the `bcrypt` cleanup: production audit warnings dropped again to `4` moderate-only issues, and the remaining chain now comes from `umzug -> @rushstack/ts-command-line -> @rushstack/terminal -> @rushstack/node-core-library -> ajv`.
- Cleared the remaining moderate production audit warnings by adding a package override for `@rushstack/node-core-library@5.20.3`, which lifts the transitive `ajv` used under `umzug -> @rushstack/ts-command-line` to the fixed `8.18.0` line without changing the current migration runner code or replacing `umzug`.
- Re-ran `npm.cmd ls umzug @rushstack/ts-command-line @rushstack/terminal @rushstack/node-core-library ajv --all`, `npm.cmd audit --omit=dev --json`, `node --test tests/api-v2/migrations-quote-table-compat.test.js` and `npm.cmd run test:ci`; all passed, and the production audit baseline is now `0 vulnerabilities`.
- Verified that local `npm run migrate:status` still requires a real `DATABASE_URL` in this environment; the command currently exits early with the existing `DATABASE_URL is required` guard, so that tooling/setup gap is tracked separately instead of being treated as a regression from the override.
- Refactored `scripts/migrate.js` so it now performs an explicit `DATABASE_URL` preflight before loading `db/migrator`, exports the preflight helpers for testing, and prints one clear CLI message instead of crashing through `config/database.js` with a raw stack trace when local migration commands are run without database env configured.
- Added `tests/api-v2/migrate-script-env.test.js`, which spawns the real migration CLI with `DATABASE_URL` removed and locks in the new fail-fast behavior: exit code `1`, a helpful `DATABASE_URL` guidance message, and no leaked `config/database.js` stack noise.
- Re-ran `node --check scripts/migrate.js`, `node --test tests/api-v2/migrate-script-env.test.js` and `npm.cmd run test:ci`; all passed after the migration-tooling preflight cleanup.
- Continued the Sonar cleanup in `tests/playwright/mobile-smoke.spec.js` by extracting a shared request-method route helper, cleaning up the responsive-nav/dashboard-toggle helpers, removing repeated inline `POST/PATCH/DELETE` route guards, and simplifying member-add logic in the manager chat smoke flow without changing test behavior.
- Cleaned up a stray test string introduced during the earlier `globalThis` portability pass, so the manager private-thread smoke now asserts the intended human message text instead of a browser-global artifact.
- Re-ran `node --check tests/playwright/mobile-smoke.spec.js`, re-ran the full focused Playwright mobile/workspace suite through `node scripts/run-playwright.js -c tests/playwright/playwright.config.js --grep "homepage mobile shell|auth page|client dashboard|manager dashboard"`, and re-ran `npm.cmd run test:ci`; all stayed green after the mobile-smoke cleanup.
- Repointed the gallery asset pipeline in `scripts/asset-optimization.config.js` to the current bathroom, kitchen and exterior source images while intentionally keeping the stable brochure-facing `/Gallery/premium/...` output names used by `asset-manifest.js`, the brochure pages and service/location content.
- Updated `gallery.js` fallback service collections to the current shipped bathroom, kitchen and exterior image set, including the new bathroom suite, wet-room geometry, kitchen trio and exterior brick/rendering images, and removed the stale empty interior fallback block from the default gallery collections.
- Rebuilt the premium gallery outputs and refreshed `asset-manifest.js`, then re-ran `npm.cmd run verify:generated` and `node scripts/run-playwright.js -c tests/playwright/playwright.config.js tests/playwright/public-redesign.spec.js`; both passed, confirming the new gallery asset pack is wired through the brochure/gallery experience without breaking public routes.
- Added [deploy/WINDOWS_POWERSHELL_5_1_LOCAL_COMMANDS.md](deploy/WINDOWS_POWERSHELL_5_1_LOCAL_COMMANDS.md) as the canonical local-shell reference for this repo, documenting `npm.cmd`, explicit fail-fast `$LASTEXITCODE` checks, `cmd /c` one-liners, and PowerShell-safe examples for generated pages, API tests, Playwright runs and local migrations.
- Updated [README.md](README.md) to point Windows users at the new PowerShell 5.1 command reference and included an inline fail-fast example, closing the recurring local-shell issue where Bash-style `&&` snippets were being pasted into PowerShell 5.1.
- Tightened the manager project-chat Playwright smoke flow in `tests/playwright/mobile-smoke.spec.js` by waiting for the extra-participant lookup to resolve and tabbing focus out of the combobox before submitting, which removes the `mobile-webkit` flake where the first tap after the email fill could be swallowed by the WebKit datalist/focus state.
- Re-ran the full local `npm.cmd run test:e2e:mobile` suite and recorded the first green local pass at `78/78`; the previous `spawn EPERM` blocker did not reproduce in the current environment, so the remaining issue was test stability in the project-chat create flow rather than child-process launch permissions.
- Ran a live production cache-header check against `https://levellines.co.uk/`, the current versioned `styles/tokens.css`, `asset-manifest.js`, `assets/optimized/brand/title.png` and `/healthz`; production currently serves `HTML`/`healthz` with `Cache-Control: no-store`, while versioned `CSS`/`JS`/image assets use `Cache-Control: max-age=604800, public`.
- Ran a live Playwright production check for `https://levellines.co.uk/services.html` -> `Discuss wall systems`, confirming the CTA still lands on `https://levellines.co.uk/quote.html?projectType=interior#quote-card` and that the quote form preselects `Interior and Exterior Wall` in `Project type`.

## 2026-03-20

- Saved `Plans/Plan Template.md`, created `Plans/Plan poprawy designu i SEO dla gotowej strony Level Lines Studio.md`, and registered that execution plan in `Plans/Plan History.md` so the launch-ready design/SEO pass has a tracked source of truth in the repo.
- Tightened the shared brochure copy in `brand.js`, `scripts/publicPages.shared.js` and `site.js`: shorter hero/session language, `Manchester and the North West` regional wording, leaner footer copy, and a new shared featured-location link set that can be reused across manual and generated public pages.
- Extended `scripts/publicPageRenderer.js` with reusable proof-chip and link-cluster renderers so brochure sections can show concise proof points and internal-route pills instead of long helper paragraphs, which keeps the current static pipeline portable for a future app/content API without adding a framework migration now.
- Expanded `scripts/servicePages.data.js` and `scripts/locationPages.data.js` with launch-ready SEO/editorial fields (`primaryKeyword`, `searchIntent`, `summaryLine`, `proofPoints`, `internalLinks`, `suppressHelperCopy`) and updated the service/location page generators to consume those fields for shorter top copy, service/local route clusters and cleaner search-intent ownership.
- Rebuilt the manual brochure pages around the lighter launch-ready content model: homepage now leads with concise proof chips and featured-location links; `services.html` acts as the main routing hub; `quote.html` drops the old instructional bands in favor of one short sales-led intro and compact next-step cards; `gallery.html` removes browsing-helper copy in favor of a tighter service-led CTA card; `contact.html`, `about.html` and the legal pages keep only short, necessary context plus route/CTA sections.
- Cleaned up `auth.html`, `client-dashboard.html` and `manager-dashboard.html` copy so the shell stays luxury-branded but task-led: shorter hero leads, less marketing filler, renamed `Quick Access` / `Account Panel` wording where appropriate, and less dense helper text around mailbox/account surfaces.
- Updated generated public pages (`premium-bathrooms-manchester.html`, `premium-kitchens-manchester.html`, `interior-renovations-manchester.html`, and all `premium-renovations-*.html` location pages) through `npm.cmd run generate:public-pages:content`, so the new region wording, proof chips, internal link clusters and shorter summary copy now match the manual brochure pages.
- Added `tests/api-v2/public-seo-contract.test.js` as an automated launch-ready SEO/content contract: it enforces unique public `title`/`meta description`, correct `canonical`, one `H1`, correct `index`/`noindex` rules, sitemap inclusion/exclusion, JSON-LD presence and the removal of the old helper-copy phrases from the brochure HTML.
- Updated `tests/playwright/mobile-smoke.spec.js` to match the renamed client-dashboard `Quick Access` heading, then re-ran the focused and full suites so the new public/content/SEO pass stays covered end to end.
- Re-ran `npm.cmd run generate:public-pages:content`, `npm.cmd run verify:generated`, `node --check brand.js`, `node --check site.js`, `node --check scripts/generate-service-pages.js`, `node --check scripts/generate-location-pages.js`, `node --test tests/api-v2/public-seo-contract.test.js`, the focused public/mobile Playwright suites, and `npm.cmd run test:ci`; all passed after the launch-ready design and SEO implementation.

## 2026-03-22

- Performed a repo-level project analysis covering the current runtime architecture, legacy/public surfaces, `api/v2`, `apps/web-v2`, `apps/mobile-v1`, migrations, deploy tooling and test coverage.
- Read the main runtime entry points (`app.js`, `server.js`, `api/v2/index.js`, core legacy routes, model associations and the v2 auth/session flow) to map the actual product shape instead of relying only on README notes.
- Verified the current execution baseline locally with `npm.cmd run test:ci`, `npm.cmd run test:e2e:mobile` and `npm.cmd run build` in `apps/web-v2`; all passed on `2026-03-22`.
- Recorded the analysis as `Plans/Analiza Projektu Building Company - Stan, Ryzyka I Kierunek.md` and registered it in `Plans/Plan History.md`.
- Added the main architecture follow-up items to `Project_todos.md`: choose one target authenticated web surface, unify auth/session ownership, decide the real role of `apps/web-v2`, remove the mobile production URL fallback, introduce a shared typed contract layer, decide migration ownership timing, and clarify the index-management strategy.
- Compared the maturity and direction of legacy dashboards versus `apps/web-v2`, then made the project-level decision to treat `web-v2` as the target authenticated web app and legacy dashboards as a transitional compatibility layer.
- Saved the decision and phased migration path in `Plans/Decyzja Docelowej Warstwy Web - web-v2 z Kontrolowanym Cutoverem.md`, registered it in `Plans/Plan History.md`, and translated the decision into concrete `Project_todos.md` follow-up tasks for rollout, parity mapping and shared-contract work.
- Implemented the first controlled rollout step for `web-v2`: `app.js` now serves the built `apps/web-v2/dist` bundle under `/app-v2`, with client-side route fallback kept behind the same mount while legacy dashboards remain online.
- Updated `apps/web-v2` build/runtime config for subpath delivery by setting the Vite base to `/app-v2/` and deriving the React Router basename from `import.meta.env.BASE_URL`, then rebuilt the tracked dist output.
- Mirrored the same `/app-v2` rollout mount in `scripts/playwright-static-server.js` so local browser smoke coverage can exercise the new shell consistently with the real Express runtime.
- Added API/runtime regression coverage in `tests/api-v2/app-legacy-routes.test.js` to lock in the `/app-v2` shell and its nested-route fallback behavior.
- Applied the requested gallery changes by removing the deleted bathroom concept image from active config, repointing the premium bathroom alias pipeline away from the missing `Gemini_Generated_Image_9cq5ti9cq5ti9cq5.png`, rebuilding `Gallery/premium/*` and refreshing `asset-manifest.js`.
- Updated `gallery.js` service fallbacks to read from stable curated `/Gallery/premium/...` aliases, preserving a 3-image roller layout for bathrooms, kitchens and exteriors even after the raw bathroom source set changed.
- Regenerated the affected generated public pages (`premium-bathrooms-manchester.html`, `premium-renovations-didsbury.html`) after the refreshed gallery manifest changed image dimensions and responsive source sets.
- Re-ran `npm.cmd run build` in `apps/web-v2`, `npm.cmd run optimize:assets`, `npm.cmd run generate:public-pages:content`, `npm.cmd run test:ci`, a focused Playwright regression for gallery/quote/direct-attachment flows, and the full `npm.cmd run test:e2e:mobile`; all passed after the rollout-route and gallery refresh work.
- Saved `Plans/Web-v2 Parity Sprint - Overview, Inbox And Notifications.md`, registered it in `Plans/Plan History.md`, and translated the parity map into concrete follow-up checklist items in `Project_todos.md`.
- Upgraded `api/v2/routes/messages.js` so thread-list responses now include latest preview, latest sender, message count, project/quote context, member count and current-user membership role, aligning the v2 contract with the new rollout inbox needs.
- Updated `api/v2/routes/messages.js` message-create/upload responses to include `sender`, which lets `web-v2` render optimistic thread updates immediately after a send instead of forcing a refetch.
- Expanded `apps/web-v2/src/lib/api.js` with thread-history fetch, text send, attachment upload and notification read/mark-all actions so the rollout shell can use real operational workflows instead of read-only placeholders.
- Rebuilt `apps/web-v2/src/App.jsx` from a placeholder panel list into a role-aware workspace shell with a real `Overview` route, richer `Projects` / `Quotes` boards, a two-pane thread inbox, actionable notifications, and stronger read-only `CRM` / `Inventory` operational surfaces.
- Reworked `apps/web-v2/src/styles.css` into a full responsive workspace design system for the rollout shell, including a darker control-room sidebar, richer card hierarchy, two-pane messaging layout and mobile-safe stacking.
- Added `tests/api-v2/messages-thread-summaries.test.js` to lock in the richer v2 thread-summary contract and optimistic message sender payload, and updated `tests/api-v2/messages-cursor-pagination.test.js` for the new thread lookup dependency.
- Rebuilt the tracked `apps/web-v2/dist` bundle after the parity sprint, producing the new hashed rollout assets for `/app-v2`.
- Re-ran `npm.cmd run build` in `apps/web-v2`, `node --test tests/api-v2/messages-thread-summaries.test.js`, `npm.cmd run test:ci` and the full `npm.cmd run test:e2e:mobile`; all passed after the parity sprint.

## 2026-03-23

- Saved `Plans/Web-v2 Parity Sprint - Quote Creation, CRM Editing And Zod Contracts.md`, registered it in `Plans/Plan History.md`, and narrowed `Project_todos.md` so the open manager-parity work now focuses on richer project lifecycle actions plus the last broader legacy-action gaps instead of still listing quote creation/CRM editing.
- Added `zod` to the runtime dependency set and promoted `shared/contracts/v2.js` from plain enum/normalizer helpers to a Zod-backed shared contract layer with runtime schemas for projects, quotes, messaging, notifications, CRM and inventory, while keeping `shared/contracts/v2.d.ts` as the TypeScript-friendly declaration surface.
- Expanded the shared quote contract with the fields needed for real create/edit parity (`projectType`, guest/contact details, budget/contact metadata) and fixed nested `project.quote` / `thread.project|quote` normalization so optional nested records now resolve as `null` instead of fake empty objects.
- Updated `apps/web-v2/src/lib/api.js` to consume the new Zod-backed shared schemas for runtime-validated list/item parsing and added the next manager-facing client helpers: quote creation plus CRM client/staff patch actions.
- Extended `api/v2/routes/quotes.js` with manager/admin quote creation, richer quote patch support, shared project-type/contact-method validation and hydrated quote responses, so `web-v2` can now create internal/guest quotes instead of only editing status/priority.
- Extended `api/v2/routes/crm.js` with `PATCH /api/v2/crm/clients/:id` and `PATCH /api/v2/crm/staff/:id`, including manager/admin guardrails for manager/admin accounts, self-deactivation protection and role-change restrictions, then documented the new routes in `api/v2/README.md`.
- Reworked `apps/web-v2/src/App.jsx` so `Quotes` now supports real create/edit flows and `CRM` now supports client/staff editing directly inside the rollout shell, with unambiguous create-vs-edit labels that keep the Playwright surface stable.
- Added `tests/api-v2/quotes-rbac-crud.test.js`, expanded `tests/api-v2/crm-rbac-crud.test.js` to cover the new patch flows, and extended `tests/playwright/web-v2-rollout.spec.js` so the rollout shell now covers quote creation plus CRM client/staff editing in addition to the earlier manager CRUD checks.
- Re-ran `node --test tests/api-v2/crm-rbac-crud.test.js`, `node --test tests/api-v2/quotes-rbac-crud.test.js`, `npm.cmd run build` in `apps/web-v2`, the focused `web-v2-rollout` Playwright suite, `npm.cmd run test:ci`, and the full `npm.cmd run test:e2e:mobile`; all passed after the quote/CRM/Zod contract sprint.
- Saved `Plans/Web-v2 Parity Sprint - Manager CRUD, Rollout Coverage And Shared Contracts.md`, registered it in `Plans/Plan History.md`, and updated `Project_todos.md` so this round clearly separates what is now done from the remaining parity/contract follow-up work.
- Added `shared/contracts/v2.js` and `shared/contracts/v2.d.ts` as the first shared contract layer for `api/v2` and `web-v2`, centralising portable enums plus response normalizers for projects, quotes, messaging, notifications, CRM and inventory while keeping the current runtime stack JavaScript-first.
- Reused the new shared contract constants inside `api/v2/routes/projects.js`, `api/v2/routes/quotes.js` and `api/v2/routes/inventory.js`, reducing enum drift between backend validation and the rollout shell before future mobile/native clients start consuming the same domains.
- Extended `api/v2/routes/crm.js` with manager/admin staff creation (`POST /api/v2/crm/staff`), shared role validation and the right manager-vs-admin permission split, then documented the new route in `api/v2/README.md`.
- Reworked `apps/web-v2/src/lib/api.js` to consume the shared contract layer and expose the first real manager operations client for `projects`, `quotes`, `crm` and `inventory`, instead of keeping those rollout sections read-only.
- Rebuilt `apps/web-v2/src/App.jsx` and `apps/web-v2/src/styles.css` so the manager-facing `Projects`, `Quotes`, `CRM` and `Inventory` routes now support selection, create/update/delete workflows, role-aware forms and mobile-safe editing surfaces through `/app-v2`.
- Fixed the rollout messaging loading loop in `Private Inbox` and `Project Chat` by stabilising the thread-loading effect dependencies around the source thread collections instead of recomputed filtered arrays, which keeps both views responsive while the new `/app-v2` Playwright coverage exercises them directly.
- Updated `apps/web-v2/vite.config.js` so the Vite build/dev pipeline can safely import the repo-root shared contract module, then rebuilt the tracked `apps/web-v2/dist` bundle for the latest `/app-v2` rollout assets.
- Added `tests/api-v2/crm-rbac-crud.test.js` to lock in the new manager/admin CRM staff-creation rules and added `tests/playwright/web-v2-rollout.spec.js` to cover `/app-v2/private-inbox`, `/app-v2/messages` and the first manager operations flows directly in the rollout shell.
- Re-ran `node --test tests/api-v2/crm-rbac-crud.test.js`, `npm.cmd run build` in `apps/web-v2`, the focused `web-v2-rollout` Playwright suite, `npm.cmd run test:ci`, and the full `npm.cmd run test:e2e:mobile`; all passed after the manager parity, rollout coverage and shared-contract work.
- Saved `Plans/Web-v2 Parity Sprint - Private Inbox And Direct Messaging.md`, registered it in `Plans/Plan History.md`, and translated the sprint outcome into updated checklist items in `Project_todos.md`.
- Extended `api/v2/routes/messages.js` with direct/private thread endpoints for list, create, history, send, attachment upload and mark-read flows, keeping the responses aligned with the v2 `ok/fail` contract and optimistic sender payloads.
- Documented the new private-thread contract in `api/v2/README.md` so the rollout shell and future mobile/native clients have one explicit API surface to follow.
- Expanded `apps/web-v2/src/lib/api.js` with the missing private-thread client helpers so `web-v2` can open direct routes, load history, send text, upload attachments and clear unread state through `api/v2`.
- Reworked `apps/web-v2/src/App.jsx` to add a real `Private Inbox` route, direct-thread summary cards, role-aware direct-thread creation, attachment-first private-thread creation, and overview/navigation updates that distinguish `Private Inbox` from `Project Chat`.
- Added `tests/api-v2/direct-thread-contract.test.js` to lock in latest-preview, unread-count, counterparty, create-only and attachment-upload behavior for the new v2 private-thread surface.
- Rebuilt the tracked `apps/web-v2/dist` bundle so the shipped `/app-v2` rollout assets include the new private-inbox experience.
- Re-ran `npm.cmd run build` in `apps/web-v2`, `node --test tests/api-v2/direct-thread-contract.test.js`, `npm.cmd run test:ci` and the full `npm.cmd run test:e2e:mobile`; all passed after the private-inbox parity sprint.
- Saved `Plans/Folder Names As Gallery Source Of Truth.md`, registered it in `Plans/Plan History.md`, and linked the completed gallery source-of-truth change back into `Project_todos.md`.
- Added `utils/folderGallery.js` as the shared folder-gallery source of truth, then rewired `utils/publicGallery.js` so `/api/gallery/services` now returns raw folder names, alphabetical folder order, alphabetical file order and skips empty folders.
- Removed the curated service-bundle override from `gallery.js`, switched the public gallery fallback rail to the canonical singular folder names (`bathroom`, `exterior`, `kitchen`), and updated `gallery.html` so its initial active heading/meta no longer references the old curated labels.
- Updated `scripts/publicPageRenderer.js`, `scripts/generate-service-pages.js` and `scripts/generate-location-pages.js` so generated service/location pages now embed the same folder-derived `data-gallery-projects-json` payload used by the folder gallery contract without changing the existing hero/media-strip brochure layout.
- Repointed `scripts/asset-optimization.config.js` to the canonical singular gallery folders and regenerated `Gallery/premium/*` plus `asset-manifest.js`, keeping the brochure-facing premium aliases compatible with the new folder layout.
- Regenerated the generated public pages, updated `tests/api-v2/legacy-public-routes.test.js` and `tests/playwright/public-redesign.spec.js` for the raw-folder gallery contract, then re-ran `npm.cmd run optimize:assets`, `npm.cmd run generate:public-pages:content`, `npm.cmd run verify:generated`, focused gallery Playwright coverage, `npm.cmd run test:ci` and the full `npm.cmd run test:e2e:mobile`; all passed after the gallery source-of-truth rollout.
- Saved `Plans/Responsive Folder Gallery Media Pipeline.md`, registered it in `Plans/Plan History.md`, and closed the matching `Project_todos.md` item for folding raw folder gallery images into the `sharp` media pipeline.
- Extended `utils/folderGallery.js` so generated `Gallery/optimized` outputs are ignored by folder-gallery scans and exposed a source-image listing helper that the asset pipeline can reuse without inventing a second gallery inventory.
- Expanded `scripts/asset-optimization.config.js` from premium-only aliases to a combined premium + raw-folder asset manifest, writing folder-driven optimized variants under `Gallery/optimized/<folder>/...` so raw `/Gallery/<folder>/<file>` URLs now resolve to responsive AVIF/WebP/JPG outputs through `asset-manifest.js`.
- Hardened `scripts/optimize-assets.js` to encode public asset URLs segment-by-segment, which keeps the new optimized gallery manifest valid even when source filenames contain spaces and are used in `srcset`.
- Updated `tests/api-v2/legacy-public-routes.test.js` to prove generated folders stay hidden from `/api/gallery/services`, and updated `tests/playwright/public-redesign.spec.js` so the gallery regression now checks that folder-driven rail images render optimized `<picture>` sources from `Gallery/optimized`.
- Re-ran `npm.cmd run optimize:assets`, `npm.cmd run verify:generated`, `node --test tests/api-v2/legacy-public-routes.test.js`, focused public gallery Playwright coverage, `npm.cmd run test:ci` and the full `npm.cmd run test:e2e:mobile`; all passed after the responsive raw-folder gallery media pipeline rollout.

## 2026-03-24

- Saved `Plans/Quotes Lifecycle System - Client Portal, Manager Workflow And Project Conversion.md`, registered it in `Plans/Plan History.md`, and translated the rollout into new completed/open checklist items in `Project_todos.md`.
- Added `utils/quoteWorkflow.js` as the shared quote lifecycle helper for workflow statuses, estimate decision statuses, legacy-status bridging and quote/project naming.
- Extended `models/Quote.js`, `models/Estimate.js`, `models/Project.js` and `models/index.js` with the new lifecycle fields (`workflowStatus`, current estimate, converted project, timestamps, accepted estimate) and added the new `models/QuoteEvent.js` timeline model plus associations.
- Added `migrations/202603240001-quote-workflow-and-events.js` to create the quote lifecycle columns, quote-event table, project accepted-estimate link and the initial backfill for existing quote rows.
- Reworked `shared/contracts/v2.js` and `shared/contracts/v2.d.ts` so the shared `api/v2` / `web-v2` contract now carries quote workflow state, estimate summaries, quote timeline events and project conversion metadata, while also removing the brittle browser-side dependency on backend `quoteWorkflow` imports by inlining the workflow constants in the shared contract layer.
- Expanded `api/v2/routes/quotes.js` into the first real lifecycle router covering client submit, manager ownership, estimate draft/send, client response, quote timeline/estimate reads and manual quote-to-project conversion after approval.
- Updated legacy quote routes in `routes/quotes.js` and `routes/manager/quote-routes.js` so guest submit/claim plus legacy manager assignment now write the new lifecycle metadata and quote events without breaking the existing public flow.
- Expanded `apps/web-v2/src/lib/api.js` with quote detail, timeline, estimate, ownership, response and project-conversion helpers for the rollout shell.
- Rebuilt `apps/web-v2/src/App.jsx` quotes workspace so managers can own/edit quotes, draft/send estimates and convert approved work into projects, while logged-in clients can review estimate state, accept/decline/request revision and submit fresh quote requests inside `/app-v2/quotes`.
- Added/updated regression coverage in `tests/api-v2/quotes-rbac-crud.test.js` and `tests/playwright/web-v2-rollout.spec.js` for the backend lifecycle plus manager/client rollout-shell quote journeys.
- Re-ran `node --test tests/api-v2/quotes-rbac-crud.test.js`, `npm.cmd run build` in `apps/web-v2`, focused Playwright quote rollout coverage for manager and client flows, `npm.cmd run test:ci`, and `npm.cmd run test:e2e:mobile`; all passed after the quote lifecycle rollout.
- Saved `Plans/Quote Workflow Production Migration Recovery Hotfix.md`, registered it in `Plans/Plan History.md`, and added the production migration recovery item to `Project_todos.md`.
- Hardened `migrations/202603240001-quote-workflow-and-events.js` so it now resumes safely after a partial apply by skipping already-created columns, indexes and the `QuoteEvents` table instead of crashing on rerun.
- Changed the quote-workflow backfill SQL in that migration to cast the `CASE` result explicitly to `enum_Quotes_workflowStatus`, which is the safer Postgres-compatible path for enum assignment during production startup migrations.
- Added migration regression coverage in `tests/api-v2/migrations-quote-table-compat.test.js` for the partial-apply recovery path and the explicit enum-cast backfill SQL before re-running the validation gates.
- Saved `Plans/Public Guest Quote Submit Hardening.md`, registered it in `Plans/Plan History.md`, and marked the public guest quote hardening item as done in `Project_todos.md`.
- Hardened `routes/quotes.js` so the public `/api/quotes/guest` flow now treats `QuoteEvent.create(...)` and manager notification fan-out as best-effort side effects, logging warnings instead of failing the customer submission after the quote itself is stored.
- Added public quote regression coverage in `tests/api-v2/legacy-public-routes.test.js` for both the normal guest quote submit path and the degraded path where quote-event / notification side effects throw.
- Re-ran `node --test tests/api-v2/legacy-public-routes.test.js` and `npm.cmd run test:ci`; both passed after the guest quote submit hardening fix.
- Saved `Plans/Public Guest Quote Compatibility Fallback.md`, registered it in `Plans/Plan History.md`, and marked the compatibility-fallback item as done in `Project_todos.md`.
- Added `createGuestQuoteRecord(...)` in `routes/quotes.js` so the public guest quote route now retries with a legacy-safe payload when the initial insert fails on the newer lifecycle columns, then attempts a best-effort lifecycle metadata backfill on the created quote.
- Extended `tests/api-v2/legacy-public-routes.test.js` with a production-style compatibility regression proving that `/api/quotes/guest` still returns `201` after a first-write failure on `workflowStatus`, followed by a successful fallback insert plus a non-blocking lifecycle update.
- Re-ran `node --test tests/api-v2/legacy-public-routes.test.js` and `npm.cmd run test:ci`; both passed after the guest quote compatibility fallback fix.
- Saved `Plans/Quotes ClientId Nullability Production Hotfix.md`, registered it in `Plans/Plan History.md`, and marked the schema hotfix item as done in `Project_todos.md`.
- Added `migrations/202603240002-relax-quotes-clientid-nullability.js` so production databases that still have `Quotes.clientId NOT NULL` are patched to `allowNull: true` during the next deploy migration run, while already-correct databases stay untouched.
- Extended `tests/api-v2/migrations-quote-table-compat.test.js` with regression coverage for the new `Quotes.clientId` nullability hotfix, proving it changes the column only when needed and becomes a no-op when the schema is already correct.
- Re-ran `node --test tests/api-v2/migrations-quote-table-compat.test.js` and `npm.cmd run test:ci`; both passed after the `Quotes.clientId` production schema hotfix.
- Saved `Plans/Quotes ClientId Force Drop Not Null Hotfix.md`, registered it in `Plans/Plan History.md`, and marked the force-drop schema hotfix as done in `Project_todos.md`.
- Added `migrations/202603240003-force-drop-quotes-clientid-not-null.js` so the live Postgres table now gets a deterministic raw `ALTER TABLE "Quotes" ALTER COLUMN "clientId" DROP NOT NULL`, which targets the exact constraint still blocking public guest quote inserts in production.
- Extended `tests/api-v2/migrations-quote-table-compat.test.js` with regression coverage for the raw-SQL `DROP NOT NULL` migration, then re-ran `node --test tests/api-v2/migrations-quote-table-compat.test.js` and `npm.cmd run test:ci`; both passed after the final `clientId` constraint hotfix.
- Saved `Plans/Quote Photo Attachments For Public And Web-v2 Intake.md`, registered it in `Plans/Plan History.md`, and marked the quote-photo intake item as done in `Project_todos.md` while opening the follow-up portal attachment item for later.
- Added `models/QuoteAttachment.js`, `utils/quoteAttachments.js` and `migrations/202603240004-quote-attachments.js`, then wired `models/index.js` so quote photos are now first-class records with quote/uploader associations and dedicated indexes.
- Extended `routes/quotes.js` so legacy public `/api/quotes/guest` now accepts `multipart/form-data`, validates up to 8 image uploads, stores quote attachment records transactionally, includes photo-count metadata in quote events/manager notifications, and returns attachment metadata in the success payload.
- Extended `api/v2/routes/quotes.js`, `shared/contracts/v2.js`, `shared/contracts/v2.d.ts`, `apps/web-v2/src/lib/api.js` and `apps/web-v2/src/App.jsx` so quote attachments are part of the typed quote payload, visible in quote detail, and uploadable from the rollout shell without breaking the existing quote/estimate/project lifecycle.
- Reworked `quote.js`, `quote.html` and `scripts/publicPageRenderer.js`, then regenerated the brochure pages so the public quote page plus generated service/location quote forms now expose optional photo upload, use multipart submit and show clearer upload-status copy around the new quote system.
- Added regression coverage in `tests/api-v2/legacy-public-routes.test.js`, `tests/api-v2/quotes-rbac-crud.test.js`, `tests/api-v2/migrations-quote-table-compat.test.js` and `tests/playwright/public-redesign.spec.js`, then re-ran `node --test tests/api-v2/legacy-public-routes.test.js`, `node --test tests/api-v2/quotes-rbac-crud.test.js`, `node --test tests/api-v2/migrations-quote-table-compat.test.js`, `npm.cmd run build` in `apps/web-v2`, `npm.cmd run generate:public-pages:content`, `npm.cmd run verify:generated`, focused public + `web-v2` Playwright quote coverage, and `npm.cmd run test:ci`; all passed after the quote-photo attachment rollout.
- Saved `Plans/Quote Multi Photo Preview And Thumbnail UX.md`, registered it in `Plans/Plan History.md`, and marked the public quote thumbnail-preview UX task as done in `Project_todos.md`.
- Reworked `quote.js`, `quote.html` and `scripts/publicPageRenderer.js` again so brochure quote forms now render live thumbnail previews for selected files, keep the multi-photo upload flow intact, and let the client remove an individual image before submit.
- Added `quote-file-preview*` styling in `styles/public.css` so the selected-photo cards match the current brochure card treatment instead of falling back to raw browser layout.
- Regenerated the service/location brochure pages and extended `tests/playwright/public-redesign.spec.js` so the public quote regression now proves multi-file preview, thumbnail rendering and single-file removal on desktop, mobile Chromium and mobile WebKit.
- Re-ran `npm.cmd run generate:public-pages:content`, `npm.cmd run verify:generated`, focused public Playwright quote coverage, and `npm.cmd run test:ci`; all passed after the quote preview UX polish.

## 2026-03-26

- Saved `Plans/Guest Quote Private Preview Link And Follow Up Panel.md`, registered it in `Plans/Plan History.md`, and marked the first public guest quote follow-up layer as done in `Project_todos.md` while keeping the bigger guest portal parity task open.
- Extended `routes/quotes.js` so `GET /api/quotes/guest/:publicToken` now returns guest preview payloads with quote attachment metadata and attachment counts, which makes the current `publicToken` useful beyond a bare status lookup.
- Reworked `quote.js`, `quote.html` and `scripts/publicPageRenderer.js` so brochure quote forms now render a private follow-up/status panel after successful submit, persist the private link in the current URL via `?quote=<publicToken>`, and restore the same panel on later visits from that private link.
- Added `quote-followup*` styling in `styles/public.css` so the new guest preview panel, metadata grid and read-only photo cards fit the existing brochure card language on desktop and mobile.
- Regenerated the generated service/location brochure pages, added API regression coverage for guest preview attachments in `tests/api-v2/legacy-public-routes.test.js`, and extended `tests/playwright/public-redesign.spec.js` so the public quote flow now proves submit -> private link -> restored preview across desktop, mobile Chromium and mobile WebKit.
- Re-ran `npm.cmd run generate:public-pages:content`, `npm.cmd run verify:generated`, focused brochure Playwright coverage for the new guest preview flow plus generated quote forms, and `npm.cmd run test:ci`; all passed after the guest quote follow-up rollout.
- Saved `Plans/Guest Quote Claim Handoff Between Quote Preview And Auth Account.md`, registered it in `Plans/Plan History.md`, and marked the new guest quote claim-handoff checklist item as done in `Project_todos.md` while keeping the broader guest quote portal parity task open.
- Extended `routes/quotes.js` again so the guest preview response now includes safe claim metadata (`claimChannels`, masked email/phone hints) and `claim/request` returns a masked delivery target alongside the new claim token.
- Reworked `quote.js` so the public follow-up/status panel now renders a claim card, can request a claim code by email or phone, persists the pending claim locally, and hands the customer off to `auth.html` without losing the saved quote context.
- Added a dedicated claim-confirm panel to `auth.html`, `auth.js` and `styles/workspace.css`, which pauses the normal post-login redirect when a pending quote claim exists and lets the customer confirm the 6-digit code before continuing to the compatible client workspace.
- Updated `styles/public.css` and `tests/playwright/public-redesign.spec.js` so the quote follow-up panel styling and browser regression coverage now include the new claim request -> login -> confirm handoff on desktop, mobile Chromium and mobile WebKit.
- Extended `tests/api-v2/legacy-public-routes.test.js` with regression coverage for the masked claim-request response, then re-ran `node --test tests/api-v2/legacy-public-routes.test.js`, the focused public Playwright quote/claim coverage, and `npm.cmd run test:ci`; all passed after the guest quote claim handoff rollout.
- Saved `Plans/Guest Quote Follow Up Photo Upload From Private Preview.md`, registered it in `Plans/Plan History.md`, and closed the guest follow-up photo-upload checklist item in `Project_todos.md` while leaving the later claimed-portal upload extension open.
- Extended `routes/quotes.js` with a new `POST /api/quotes/guest/:publicToken/attachments` route plus shared guest-preview builders so a guest quote can append more image attachments after the initial submission and immediately get a refreshed private preview payload back.
- Reworked `quote.js` so the private quote follow-up panel now renders a dedicated upload card with remaining-slot copy, multi-file thumbnail previews, per-file removal before upload, preserved contact/claim context across rerenders, and a refreshed saved-photo list after a successful follow-up upload.
- Added follow-up upload styling in `styles/public.css` and extended `tests/playwright/public-redesign.spec.js` so the private guest quote regression now proves add-more-photos behavior across desktop, mobile Chromium and mobile WebKit.
- Extended `tests/api-v2/legacy-public-routes.test.js` with regression coverage for the new guest follow-up attachment endpoint, including refreshed attachment counts, quote-event creation and manager notifications after extra photos are added.
- Re-ran `node --test tests/api-v2/legacy-public-routes.test.js`, `node scripts/run-playwright.js -c tests/playwright/playwright.config.js --grep "quote page shows a private guest quote preview after submit and from the saved quote link|guest quote private preview lets the customer add more photos after submit"`, and `npm.cmd run test:ci`; all passed after the guest follow-up upload rollout.
- Saved `Plans/Quote File Picker Append And Thumbnail Preview Hotfix.md`, registered it in `Plans/Plan History.md`, and closed the quote file-picker append / thumbnail-preview hotfix items in `Project_todos.md`.
- Reworked `quote.js` again so both the initial quote form and the guest follow-up upload card keep their own selected-file state, append newly picked files instead of replacing the current selection, preserve per-file removal, and submit the merged file list reliably.
- Updated `app.js` to allow `blob:` in the public `img-src` CSP, which restores browser-side local image previews for quote thumbnails before upload without changing the upload API contract.
- Extended `tests/playwright/public-redesign.spec.js` so the brochure quote regression now proves append-on-second-pick behavior and checks that preview thumbnails actually load, not just that `<img>` tags exist.
- Re-ran `node scripts/run-playwright.js -c tests/playwright/playwright.config.js --grep "core brochure pages render about, services, gallery, contact and quote routes|guest quote private preview lets the customer add more photos after submit"` and `npm.cmd run test:ci`; both passed after the quote picker append / thumbnail hotfix.
- Saved `Plans/Web-v2 Overview Aggregation, Claimed Quote Follow Up Uploads And Mobile API Environment Hardening.md`, registered it in `Plans/Plan History.md`, and closed the matching `Project_todos.md` items for the aggregated overview contract, claimed-portal follow-up uploads and explicit mobile API environment config.
- Added `api/v2/routes/overview.js`, mounted it from `api/v2/index.js`, and introduced a typed dashboard summary that aggregates role-aware projects, quotes, thread previews, notifications, CRM counts, low-stock materials and public services into one `GET /api/v2/overview` payload.
- Extended `shared/contracts/v2.js` and `shared/contracts/v2.d.ts` with the new overview schemas/normalizers, then rewired `apps/web-v2/src/lib/api.js` and `apps/web-v2/src/App.jsx` so the rollout-shell overview stops composing the top board from many separate requests.
- Extended `api/v2/routes/quotes.js` and `tests/api-v2/quotes-rbac-crud.test.js` so authenticated/claimed quote detail now supports follow-up photo uploads with the same total-photo cap enforcement as the guest private-link flow.
- Reworked `apps/web-v2/src/App.jsx` so the logged-in quotes surface can append new reference photos to an existing quote, preview the pending selection, keep the current quote selected, and refresh the saved attachment rail after a successful upload.
- Replaced the implicit production fallback in `apps/mobile-v1/src/api.js` with explicit environment-based API-base resolution and updated `apps/mobile-v1/app.json` to declare `local`, `staging` and `production` endpoints instead of silently defaulting to live production.
- Added `tests/api-v2/overview-contract.test.js` and extended `tests/playwright/web-v2-rollout.spec.js` so both the new aggregated overview contract and the claimed/logged-in follow-up quote photo flow are protected by API + browser regression coverage.
- Added dedicated DOM hooks for the public quote claim card and the guest follow-up upload card in `quote.js`, then updated `tests/playwright/public-redesign.spec.js` so the claim-handoff regression stays stable even after the private quote panel gained a second card with similar styling.
- Re-ran `node --test tests/api-v2/overview-contract.test.js`, `node --test tests/api-v2/quotes-rbac-crud.test.js`, focused public quote Playwright coverage, `npm.cmd run build` in `apps/web-v2`, `npm.cmd run test:ci` and the full `npm.cmd run test:e2e:mobile`; all passed, finishing at `102/102` Playwright checks green.
- Saved `Plans/Auth Session Bridge, Deploy-time Migrations And Project Lifecycle Hardening.md`, registered it in `Plans/Plan History.md`, and closed the matching `Project_todos.md` items for auth/session bridging, deploy-time migrations, explicit index setup, local Compose bootstrap and the new design source-of-truth doc.
- Added `utils/sessionTokens.js` and rewired `routes/auth.js`, `api/v2/routes/auth.js`, `runtime.js`, `auth.js`, `site.js` and `apps/web-v2/src/lib/api.js` so legacy auth and `api/v2` auth now issue/sync compatible session payloads across brochure pages, legacy dashboards and `web-v2` during the cutover period.
- Removed automatic migration execution from `server.js`, added `scripts/ensure-indexes.js` plus the new `npm run ensure:indexes` package script, and updated `README.md`, `deploy/CUTOVER_CHECKLIST_STAGING_PROD_v2.md` and `deploy/WINDOWS_POWERSHELL_5_1_LOCAL_COMMANDS.md` so deploys now run explicit `migrate` and optional `ensure:indexes` steps before `pm2 restart`.
- Added `deploy/docker-compose.local-db.yml` and `deploy/LOCAL_POSTGRES_COMPOSE_BOOTSTRAP.md` to document a reproducible local Postgres bootstrap path for the full app/runtime flow, then added `Current_Web_Design_Source_Of_Truth.md` as the replacement design-source document for the current brochure/workspace direction.
- Extended `api/v2/routes/projects.js`, `apps/web-v2/src/lib/api.js`, `apps/web-v2/src/App.jsx` and `tests/playwright/web-v2-rollout.spec.js` with richer manager project lifecycle/archive/delete actions and backend guards, which closes another part of the remaining legacy-only manager project flow gap without claiming the full parity item yet.
- Added regression coverage in `tests/api-v2/auth-flow.test.js`, `tests/api-v2/legacy-auth-session-bridge.test.js`, `tests/api-v2/ensure-indexes-script-env.test.js` and `tests/api-v2/projects-rbac-lifecycle.test.js`, then re-ran those targeted suites plus `npm.cmd run build` in `apps/web-v2`, `npm.cmd run test:ci` and the full `npm.cmd run test:e2e:mobile`; all passed with Playwright finishing at `102/102`.
- Saved `Plans/CRM Lifecycle, Activity Feed And V2 Public Quote Portal.md`, registered it in `Plans/Plan History.md`, and updated `Project_todos.md` so the explicit CRM lifecycle, durable activity feed and current v2 public-quote-path rollout are tracked honestly against the remaining portal parity work.
- Added `utils/crmLifecycle.js`, `utils/activityFeed.js`, `models/ActivityEvent.js` and `migrations/202603260001-crm-lifecycle-and-activity-feed.js`, then extended `models/User.js` and `models/index.js` so CRM lifecycle state and durable activity events are now persisted with the right associations and index ownership.
- Extended `shared/contracts/v2.js` and `shared/contracts/v2.d.ts` with CRM lifecycle constants plus activity-event schemas/normalizers, keeping the new workflow surface portable for `web-v2` now and a future native client later.
- Added `api/v2/routes/activity.js`, mounted it from `api/v2/index.js`, and extended `api/v2/routes/crm.js`, `api/v2/routes/quotes.js`, `api/v2/routes/projects.js` and `routes/quotes.js` so quote, estimate, CRM and project actions now emit durable company/client/project timeline events and advance client lifecycle state where appropriate.
- Added `api/v2/routes/public-quotes.js`, then rewired `quote.js` and `auth.js` so the live public quote browser flow now hits `/api/v2/public/quotes` for submit, preview, follow-up upload and claim confirmation while the current guest router remains only as a temporary adapter underneath.
- Reworked `apps/web-v2/src/App.jsx` and `apps/web-v2/src/lib/api.js` so `Overview`, `Projects` and `CRM` now render company/project/client activity surfaces and expose manager-editable CRM lifecycle state directly in the rollout shell.
- Added `tests/api-v2/activity-feed.test.js` and `tests/api-v2/public-quotes-v2-contract.test.js`, updated `tests/api-v2/crm-rbac-crud.test.js`, `tests/playwright/public-redesign.spec.js` and `tests/playwright/web-v2-rollout.spec.js`, and re-ran `npm.cmd run build` in `apps/web-v2`, all targeted API tests, `npm.cmd run test:ci`, focused public + rollout Playwright suites, and the full `npm.cmd run test:e2e:mobile`; all passed with Playwright finishing at `102/102`.
