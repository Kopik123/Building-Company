services:
  app:
    image: building-company:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: sh -c "npm run migrate && npm start"
    environment:
      NODE_ENV: staging
      PORT: 3000
    ports:
      - "3001:3000"
    restart: unless-stopped
